"""
Shared pytest fixtures for ExtraccionDatosNE test suite.

Fixtures defined here are available to all test modules automatically.
They cover:
- Mocking the Gemini GenerativeModel (mock_gemini_client)
- Sample file paths for each supported format (populated in task 5.1)
- DataFrame factory helpers (populated in task 5.1)

NOTE: This is a skeleton. Fixtures will be fully implemented in Phase 5 (task 5.1).
"""

import pytest

# ---------------------------------------------------------------------------
# Placeholder — fixtures will be filled in task 5.1
# ---------------------------------------------------------------------------

# @pytest.fixture
# def mock_gemini_client(mocker):
#     """Mock genai.GenerativeModel so tests never make real API calls."""
#     ...

# @pytest.fixture
# def sample_xlsx_path(tmp_path):
#     """Return a Path to a minimal .xlsx test fixture."""
#     ...

# @pytest.fixture
# def sample_xls_path(tmp_path):
#     """Return a Path to a minimal .xls test fixture."""
#     ...

# @pytest.fixture
# def sample_ods_path(tmp_path):
#     """Return a Path to a minimal .ods test fixture."""
#     ...

# @pytest.fixture
# def sample_pdf_path(tmp_path):
#     """Return a Path to a minimal .pdf test fixture."""
#     ...

# @pytest.fixture
# def sample_html_path(tmp_path):
#     """Return a Path to a minimal .html test fixture."""
#     ...

# @pytest.fixture
# def sample_image_path(tmp_path):
#     """Return a Path to a minimal .png test fixture."""
#     ...
