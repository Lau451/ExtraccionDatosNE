# Apply Progress - PCP Frontend

## Cumulative completion record

- **Status / safety context:** parent-authoritative `pcp-frontend`, OpenSpec, apply-ready status was consumed for every recorded frontend slice. The repository root `C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE` was the allowed edit root. No SDD authority command was run.
- **Persisted task state:** 11 implementation rows are visibly `[x]` in `tasks.md`: all five backend selection-persistence rows in section 1 and all six Gestion rows in section 2. The rows were marked when their work units completed.
- **Completed backend selection prerequisite:** added the non-exclusive persisted `seleccionado` contract, migration pair, negotiation endpoint/service/repository/model coverage, and the tenant-safe superadmin fixture correction. After the migration was applied to the guarded test project, `./venv/Scripts/python.exe -m pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` passed **19 tests** with **14 warnings**.
- **Completed Gestion foundation:** added typed PCP API contracts, PCP roles, centralized query keys, guarded PCP routes and Sidebar entry, then list/create behavior. Foundation verification included `cd frontend && npm run test -- src/features/pcp/roles.test.ts src/lib/api/pcp.test.ts src/features/pcp/GestionPcp.test.tsx src/features/pcp/PcpDetalle.test.tsx` and `cd frontend && npm run build`.
- **Completed detail, lifecycle, close, and cache objective:** added PCP detail, one-location close control, renglones Negociado/Pendiente summary, next-only state transitions, cache merge behavior, and focused detail coverage. Before this corrective rerun, the required PCP suite passed **4 files / 17 tests**, the production build passed, and `git diff --check` passed.
- **Previous deviations / blockers:** no product behavior deviation. The earlier backend integration blocker was test-project DNS and then an unapplied migration; it was resolved by applying the migration through the authorized user path. No unresolved action-context warning remains.

## Corrective Rerun - Accessible PCP state stepper

- **Status consumed:** parent-authoritative `pcp-frontend`, OpenSpec, apply-ready status for objective `gestion-detail-state-close-cache`; the repository root was the sole allowed edit root. Per instruction, no SDD status or authority command was queried.
- **Completed work / persisted checkboxes:** corrected the independent verifier's state-stepper finding. No new task row was completed in this correction: the section-2 lifecycle rows remain visibly `[x]` in `tasks.md` from the initial objective implementation.
- **Files changed:** `frontend/src/features/pcp/EstadoPcpStepper.tsx`, `frontend/src/features/pcp/PcpDetalle.test.tsx`, and this progress record. No Catalogo or unrelated behavior changed.
- **Behavior:** the stepper now renders the four ordered stages (`Nueva`, `En gestion`, `Esperando respuesta`, `Cerrada`) as an accessible named list. Each stage exposes and visibly shows `completada`, `actual`, or `pendiente`; the current stage uses `aria-current="step"`. The existing next-only mutation remains the only mutation control, and read-only behavior and close/cache contracts are unchanged.
- **Verification:** safety net `cd frontend && npm run test -- src/features/pcp/PcpDetalle.test.tsx` -> **1 file / 4 tests passed**. RED after adding stage assertions -> **1 file / 2 failures, 4 passes** because the named lifecycle list did not exist. GREEN / TRIANGULATE -> same focused command **1 file / 6 tests passed**. Required PCP suite `cd frontend && npm run test -- src/features/pcp/roles.test.ts src/lib/api/pcp.test.ts src/features/pcp/GestionPcp.test.tsx src/features/pcp/PcpDetalle.test.tsx` -> **4 files / 19 tests passed**. `cd frontend && npm run build` -> passed. `git diff --check` -> passed; it reported only pre-existing CRLF conversion warnings on unrelated tracked files.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| Accessible discrete state stepper correction | `frontend/src/features/pcp/PcpDetalle.test.tsx` | Component integration | 4/4 passed | Named stage list missing: 2 new assertions failed | 6/6 passed after rendering the ordered list | `nueva` proves current/upcoming; `en_gestion` proves completed/current/upcoming | None needed; the ordered stage constant keeps mutation and display order aligned |

- **Workload / PR boundary:** correction delta is **+48/-4 = 52 changed lines** (`PcpDetalle.test.tsx` +22/-1; `EstadoPcpStepper.tsx` +26/-3). The initial objective was **242 changed lines**, yielding **294/400** total; no size exception is needed. Rollback boundary is only the two corrected frontend files and this progress entry.
- **Design deviation / action-context warnings:** none.
- **Remaining implementation tasks:** unchanged and outside this corrective scope. The next exact unchecked row is: `- [ ] Create failing frontend/src/features/pcp/GestionCatalogoProveedores.test.tsx cases for populated and empty product catalogs, write-role association creation and refresh, hidden read-only control, and surfaced duplicate conflict; verify with cd frontend && npm run test -- src/features/pcp/GestionCatalogoProveedores.test.tsx; rollback the new test file. <!-- sdd-owner: implementation -->` All later Catalogo through cross-unit validation rows remain unchecked in `tasks.md`.

## Work Unit 3 - Catálogo

- **Status / safety context:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (11/39 complete), token `sha256:98a60cb25aa44457398ee10b905060e5d99a54a8690b925f70635e6f47d91583`, and the sole allowed edit root. No SDD status or authority command was queried.
- **Completed / persisted checkboxes:** section 3 rows 3.1–3.4 are visibly `[x]` in `tasks.md`.
- **Files changed:** `frontend/src/features/pcp/GestionCatalogoProveedores.tsx`, `GestionCatalogoProveedores.test.tsx`, `AgregarProveedorProductoDialog.tsx`, `lib/api/pcp.ts`, `_authenticated.pcp.catalogo.tsx`, generated `routeTree.gen.ts`, `tasks.md`, and this record.
- **Behavior:** typed catalog reads/writes use the existing product and tercero lookup clients; the dialog invalidates only `pcpQueryKeys.proveedoresProducto(productoId)` after success. Write control ownership is computed once in the screen and passed to the dialog, while a duplicate error remains visible and does not alter the cached list.
- **Verification:** API-client safety net `cd frontend && npm run test -- src/lib/api/pcp.test.ts` → **1 file / 4 tests passed**. RED `cd frontend && npm run test -- src/features/pcp/GestionCatalogoProveedores.test.tsx` → **1 unresolved-import suite failure**. Final focused command → **1 file / 5 tests passed**. `cd frontend && npm run build` → **passed**.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 3.1–3.4 Catálogo | `GestionCatalogoProveedores.test.tsx` | Component integration | `pcp.test.ts`: 4/4 | Missing catalog component caused the focused suite to fail | 4/4 after typed client, screen, dialog, and guarded route | Added selected-product association case; 5/5 | Simplified the conflict assertion to verify the visible list after dialog closure; 5/5 |

- **Workload / PR boundary:** Catálogo only; **331 changed lines** (+327/-4, including generated route and OpenSpec evidence), below the 400-line budget. Rollback boundary: the catalog screen, dialog, route, catalog API additions, generated route entry, catalog test, and section-3 records only. No commit, push, or PR was created.
- **Design deviations / action-context warnings:** none. CodeGraph exploration timed out after confirming the existing index, so direct reads were used as the documented fallback.
- **Remaining implementation tasks:** Renglones is next and was not started. Exact unchecked rows:
  - [ ] Create failing `frontend/src/features/pcp/RenglonDetalle.test.tsx` and extend `frontend/src/lib/api/pcp.test.ts` for renglón creation with exactly `item_proceso_id`, optional `origen`, and optional `regla_pcp_id`, normalized FastAPI 422 feedback, enriched product/supplier detail, multi-provider selection, and absent read-only controls; verify with `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/lib/api/pcp.test.ts`; rollback the renglón test additions. <!-- sdd-owner: implementation -->
  - [ ] Add typed renglón/detail/supplier-selection methods and 422-message normalization to `frontend/src/lib/api/pcp.ts`, ensuring no form-object spread can add a forbidden request key; verify the exact-body and validation RED cases pass; rollback the renglón API additions. <!-- sdd-owner: implementation -->
  - [ ] Implement `frontend/src/features/pcp/CrearRenglonDialog.tsx`, `RenglonDetalle.tsx`, `SeleccionProveedoresSection.tsx`, and `frontend/src/routes/_authenticated.pcp.$pcpId.renglones.$renglonId.tsx`; connect PCP detail to creation/listing and detail navigation, preserving read-only rendering and targeted query invalidation; verify the renglón component tests pass; rollback these components and nested route only. <!-- sdd-owner: implementation -->
  - [ ] Add renglón tests for zero catalogued suppliers, partial multi-selection, and server-validation errors rendered inline without silently dropping fields; verify with the focused renglón/API Vitest command; rollback the added edge-case tests. <!-- sdd-owner: implementation -->
  - [ ] Refactor renglón query ownership so parent detail keeps PCP summary data and child detail owns enriched renglón data; verify focused Vitest and `cd frontend && npm run build`; rollback the renglón-only refactor. <!-- sdd-owner: implementation -->
  - [ ] Create failing `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx` cases for loading, no catalogued providers, per-provider 404-as-no-result, `precio_obtenido`, `no_cotiza`, desktop columns, mobile cards, absent read-only mutations, no close control, and two simultaneous selected providers; verify with `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx`; rollback the new comparison test file. <!-- sdd-owner: implementation -->
  - [ ] Extend `frontend/src/lib/api/pcp.ts` with typed result GET/POST and `actualizarSeleccion` PATCH methods, then implement `frontend/src/features/pcp/ComparacionProveedoresTable.tsx` and `RegistrarResultadoDialog.tsx` using a bounded `useQueries` fan-out over catalogued providers and treating only `ApiError.status === 404` as no result; verify comparison RED cases pass; rollback the negotiation components and API additions. <!-- sdd-owner: implementation -->
  - [ ] Wire `RenglonDetalle.tsx` to the comparison components so write users can register outcomes and toggle the persisted, non-exclusive selection while mutations are pending, with no local selection state and no `cerrarPcp` import; verify the selection-control cases pass; rollback the renglón-detail integration and negotiation components only. <!-- sdd-owner: implementation -->
  - [ ] Add QueryClient cases proving `actualizarSeleccion` writes the returned row to the exact existing result key and normally invalidates only `['pcp', pcpId, 'seleccion']`, allowing the Gestión Negociado badge to refresh after navigation; verify focused comparison and Gestión-detail Vitest tests; rollback the added cache tests. <!-- sdd-owner: implementation -->
  - [ ] Refactor comparison rendering into shared criterion data for table and mobile cards while retaining accessible selected labels and multi-select semantics; verify `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx src/features/pcp/PcpDetalle.test.tsx` and `cd frontend && npm run build`; rollback the comparison-only refactor. <!-- sdd-owner: implementation -->
  - [ ] Create failing `frontend/src/features/pcp/ConsultaDetalle.test.tsx` and extend `frontend/src/lib/api/pcp.test.ts` for grouping selected renglones into one consultation per provider, read-only hidden grouping/send actions, binary PDF download, and send success/failure feedback without messaging-adapter detail; verify with `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/lib/api/pcp.test.ts`; rollback the consultation test additions. <!-- sdd-owner: implementation -->
  - [ ] Add typed consultation grouping/detail/send methods and the auth-header blob download implementation to `frontend/src/lib/api/pcp.ts`, then implement `frontend/src/features/pcp/AgruparConsultaDialog.tsx`, `ConsultaDetalle.tsx`, and `frontend/src/routes/_authenticated.pcp.consultas.$consultaId.tsx`; expose the grouping entry only from eligible PCP/renglón flow and target cache updates to consultation keys; verify consultation RED cases pass; rollback consultation components, route, and API additions. <!-- sdd-owner: implementation -->
  - [ ] Add consultation tests for several providers yielding several returned consultations, a PDF fetch failure, and send failure recovery that keeps the action usable; verify focused Vitest and `cd frontend && npm run build`; rollback the added edge-case tests. <!-- sdd-owner: implementation -->
  - [ ] Refactor download cleanup and mutation feedback without exposing adapter internals or adding a nonexistent consultation index route; verify the focused consultation test command; rollback the consultation-only refactor. <!-- sdd-owner: implementation -->
  - [ ] Create failing `frontend/src/features/pcp/SugerenciasPanel.test.tsx` cases for grouping and recent-price display, no-data state, read-role visibility, and the absence of any PCP mutation control; verify with `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx`; rollback the new suggestions test file. <!-- sdd-owner: implementation -->
  - [ ] Add typed grouping and recent-price reads to `frontend/src/lib/api/pcp.ts`, implement `frontend/src/features/pcp/SugerenciasPanel.tsx`, and mount it in `frontend/src/features/pcp/RenglonDetalle.tsx` with its dedicated query keys and loading/error/empty states; verify the suggestions RED cases pass; rollback the panel and its API/detail integrations. <!-- sdd-owner: implementation -->
  - [ ] Add suggestions tests for a valid recent-price row and multiple open-renglón quantity aggregation, then run focused Vitest plus `cd frontend && npm run build`; rollback the added suggestions cases. <!-- sdd-owner: implementation -->
  - [ ] Refactor suggestions rendering to remain read-only and isolated from renglón mutations; verify the focused suggestions test command; rollback the suggestions-only refactor. <!-- sdd-owner: implementation -->
  - [ ] Create failing `frontend/src/features/pcp/ImportLegacyPcp.test.tsx` cases for write-role import submission, read-only route denial, human-readable result summary, and repeat-import updated-not-duplicated feedback; verify with `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx`; rollback the new import test file. <!-- sdd-owner: implementation -->
  - [ ] Add the typed legacy-import method to `frontend/src/lib/api/pcp.ts`, implement `frontend/src/features/pcp/ImportLegacyPcp.tsx`, and create `frontend/src/routes/_authenticated.pcp.imports.tsx` with an explicit write-role guard; render result counts/statuses without raw API payloads; verify the import RED cases pass; rollback the import screen, route, and API addition. <!-- sdd-owner: implementation -->
  - [ ] Add import tests for mixed created/updated result rows and API failure recovery, then run focused Vitest plus `cd frontend && npm run build`; rollback the added import edge cases. <!-- sdd-owner: implementation -->
  - [ ] Refactor import result presentation without changing idempotency semantics or broadening route access; verify the focused import test command; rollback the import-only refactor. <!-- sdd-owner: implementation -->
  - [ ] Run the frontend PCP suite and production build with `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts && npm run build`, resolving only PCP-change failures; rollback by reverting the failing unit at its documented work-unit boundary. <!-- sdd-owner: implementation -->
  - [ ] Run the backend selection prerequisite and repository-required suite with `pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py` followed by `pytest --cov=services --cov-report=html`; record any unrelated baseline failures separately rather than modifying unrelated untracked artifacts; rollback only PCP selection-persistence changes if regression is attributable to this change. <!-- sdd-owner: implementation -->


## Corrective Rerun - Empty product catalog

- **Status / safety:** consumed parent-authoritative OpenSpec `pcp-frontend` apply-ready context, token `sha256:279e57e2bc32aa24918ca5660f176e7d6ce018a6ec50854c019c971f6f5dc14d`, with the stated three allowed edit surfaces; no SDD authority command was queried.
- **Completed / persisted checkboxes:** this corrects completed Catalogo behavior; section 3's four implementation rows remain visibly `[x]` in `tasks.md` (15/39 expected total), so no task checkbox changed.
- **Files changed:** `frontend/src/features/pcp/GestionCatalogoProveedores.tsx`, `GestionCatalogoProveedores.test.tsx`, and this record only.
- **Behavior:** after `listarProductos()` resolves `[]`, the screen shows `No hay productos disponibles.`, disables the product selector, omits the add-association action, and suppresses the disabled association query's pending indicator. Existing product catalog behavior and precise association invalidation are unchanged.
- **Verification:** safety net focused catalog test: **1 file / 5 tests passed**. RED after the empty-product test: **1 file / 1 failure, 5 passes** (missing empty-product state, rendered `Cargando proveedores…`). GREEN/TRIANGULATE: **1 file / 6 tests passed**. `cd frontend && npm run build` passed. `git diff --check` passed (only pre-existing unrelated CRLF warnings).

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| Empty product catalog correction | `GestionCatalogoProveedores.test.tsx` | Component integration | 5/5 passed | Empty-state assertion failed | 6/6 passed | Existing populated and empty-associations cases exercise the alternate product paths | None needed |

- **Workload / PR boundary:** Catalogo corrective slice only; source/test delta is **+19/-5 = 24 changed lines** and the progress record adds 17 lines, for **+36/-5 = 41 changed lines** within the hard 69-line remaining budget. Rollback boundary: the empty-product rendering and its focused test only. No commit, delivery, or later work unit was started.
- **Design deviations / action-context warnings:** none. Remaining work is unchanged, beginning with the unchecked Renglones rows in section 4.

## Work Unit 4 — Renglones 4.1 RED

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` context: apply-ready, 15/39 tasks complete, with section 4.1 RED as the assigned bounded subunit. The four user-provided edit surfaces were honored; no SDD status, attempt, review, or delivery authority command was run. No action-context warnings were supplied.
- **Completed / persisted checkbox:** marked section 4.1 RED visibly `[x]` in `tasks.md` immediately after writing the intended failing tests.
- **Files changed:** `frontend/src/features/pcp/RenglonDetalle.test.tsx`, `frontend/src/lib/api/pcp.test.ts`, `tasks.md`, and this record. No production component, API method, or route was added.
- **RED evidence:** API-client safety net before edits: `cd frontend && npm run test -- src/lib/api/pcp.test.ts` → **1 file / 4 tests passed**. Required focused command after test additions: `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/lib/api/pcp.test.ts` → **2 files failed; existing 4 API tests passed; 3 new API tests failed** because `crearRenglon` does not exist, and the new component suite failed to resolve missing `RenglonDetalle`. The new API contracts assert an exact mandatory-only body, optional `origen`/`regla_pcp_id` body, and readable `item_proceso_id: Field required` FastAPI-422 normalization. The component contract defines enriched product/catalogue rendering, two-provider selection submission, and read-only control absence.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 4.1 RED renglón client and detail | `frontend/src/features/pcp/RenglonDetalle.test.tsx`, `frontend/src/lib/api/pcp.test.ts` | Component integration + API contract | API client: 4/4 passed | Focused suite fails against missing component and `crearRenglon` | Not run — RED-only assigned subunit | Three distinct API payload/error cases plus populated, multi-select, and read-only component cases | None — no production code in this subunit |

- **Workload / PR boundary:** bounded RED-only Renglones slice; rollback is only the two test additions plus these task/progress records. It remains below the 400-line objective budget; no commit, PR, delivery, or later task was started.
- **Design deviations:** none.
- **Remaining implementation tasks:** `- [ ] Add typed renglón/detail/supplier-selection methods and 422-message normalization to \`frontend/src/lib/api/pcp.ts\`, ensuring no form-object spread can add a forbidden request key; verify the exact-body and validation RED cases pass; rollback the renglón API additions. <!-- sdd-owner: implementation -->` All subsequent unchecked rows remain unchanged.

## Work Unit 4 — Renglones 4.2 GREEN (API slice)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the bounded first 4.2 GREEN row. The permitted edit surfaces were honored; no SDD status, attempt, review, or delivery authority command was run.
- **Completed / persisted checkbox:** marked only the first section 4.2 GREEN implementation row visibly `[x]` in `tasks.md` after the focused API tests passed.
- **Files changed:** `frontend/src/lib/api/pcp.ts`, `tasks.md`, and this cumulative record. `RenglonDetalle.test.tsx` remains unresolved/RED and no component, route, dialog, or negotiation API was added.
- **Behavior:** added typed renglón creation, enriched detail, and supplier-selection methods. Creation builds an exact allowlisted body from `item_proceso_id`, then explicitly adds optional `origen` and `regla_pcp_id`; it never spreads a form object. FastAPI 422 detail arrays are rethrown as readable field messages.
- **Verification:** RED before production changes: `cd frontend && npm run test -- src/lib/api/pcp.test.ts` → **3 new API failures / 4 existing passes**, all due to missing `crearRenglon`. GREEN/TRIANGULATE: same required command → **1 file / 7 tests passed**. The three pre-existing RED contracts cover mandatory-only body, optional-key body, and 422 normalization.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net / RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|
| 4.2 typed renglón API | `frontend/src/lib/api/pcp.test.ts` | API contract | 4/4 prior cases passed; 3 RED cases failed on missing client method | 7/7 passed | Mandatory-only and optional-field request bodies plus the 422 validation branch | None needed; explicit body construction is the smallest readable allowlist |

- **Workload / PR boundary:** first 4.2 GREEN API-only slice; production delta is 93 added lines, below the 400-line budget. Rollback boundary is the renglón API additions and these two OpenSpec records. No commit, delivery action, or later work-unit task was started.
- **Design deviations / action-context warnings:** none.
- **Remaining implementation tasks:** the next exact unchecked implementation row is `- [ ] Implement `frontend/src/features/pcp/CrearRenglonDialog.tsx`, `RenglonDetalle.tsx`, `SeleccionProveedoresSection.tsx`, and `frontend/src/routes/_authenticated.pcp.$pcpId.renglones.$renglonId.tsx`; connect PCP detail to creation/listing and detail navigation, preserving read-only rendering and targeted query invalidation; verify the renglón component tests pass; rollback these components and nested route only. <!-- sdd-owner: implementation -->`. The unresolved `RenglonDetalle.test.tsx` RED suite is intentionally reserved for that next component slice.

## Work Unit 4 — bounded 4.2 GREEN detail and supplier selection subunit

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (17/39 complete) for this bounded GREEN subunit. The permitted edit surfaces were honored; no SDD status, attempt, review, or delivery authority command was run.
- **Completed work / persisted checkbox:** created `RenglonDetalle.tsx` and `SeleccionProveedoresSection.tsx`. The enclosing second 4.2 task is deliberately still `[ ]`; no `tasks.md` edit was made because its dialog, parent-list wiring, and nested route remain outside this subunit.
- **Behavior:** the detail query uses `pcpQueryKeys.renglon(pcpId, renglonId)` with `obtenerDetalleRenglon`, renders the enriched product identity and catalogued suppliers, and has explicit loading and error states. Centralized PCP write roles gate multi-select controls. On a successful `seleccionarProveedores` POST, only the exact current renglón key is invalidated.
- **Files changed:** `frontend/src/features/pcp/RenglonDetalle.tsx`, `frontend/src/features/pcp/SeleccionProveedoresSection.tsx`, and this cumulative record.
- **Verification:** required RED command before production code: `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/lib/api/pcp.test.ts` → component suite failed to resolve the missing `RenglonDetalle`; API suite passed **7/7**. GREEN/TRIANGULATE: same command → **2 files / 10 tests passed**.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net / RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|
| Bounded 4.2 enriched detail and supplier selection | `frontend/src/features/pcp/RenglonDetalle.test.tsx` | Component integration | Existing RED contracts failed on missing detail component; API safety net 7/7 | 10/10 focused tests passed | Populated identity/list, multi-provider POST, and read-only rendering exercise distinct paths | None needed; component boundaries keep query and mutation ownership clear |

- **Workload / PR boundary:** bounded Renglones component subunit only; 109 new source lines plus this concise evidence remain below the 400-line budget. Rollback boundary is the two new components and this progress entry. No dialog, PCP detail modification, route/routeTree work, commit, delivery action, or later task was started.
- **Design deviations / action-context warnings:** none.
- **Remaining implementation task (intentionally unchecked):** `- [ ] Implement `frontend/src/features/pcp/CrearRenglonDialog.tsx`, `RenglonDetalle.tsx`, `SeleccionProveedoresSection.tsx`, and `frontend/src/routes/_authenticated.pcp.$pcpId.renglones.$renglonId.tsx`; connect PCP detail to creation/listing and detail navigation, preserving read-only rendering and targeted query invalidation; verify the renglón component tests pass; rollback these components and nested route only. <!-- sdd-owner: implementation -->`

## Corrective Rerun — Exact renglón invalidation

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context and native PROCEED token for this correction. Only the three supplied edit surfaces were changed; no SDD status, authority, review, or delivery command was run.
- **Behavior:** successful supplier selection now invalidates only `pcpQueryKeys.renglon(pcpId, renglonId)` with `exact: true`.
- **Focused coverage:** `RenglonDetalle.test.tsx` spies on the real QueryClient and proves a successful two-provider selection calls `invalidateQueries({ queryKey: ['pcp', 'pcp-1', 'renglones', 'reng-1'], exact: true })`.
- **Verification:** safety net passed **2 files / 10 tests**. RED failed exactly because the invalidation lacked `exact: true` (**1 failure, 9 passes**). GREEN/TRIANGULATE passed `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/lib/api/pcp.test.ts` with **2 files / 10 tests**.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| Exact renglón invalidation correction | `frontend/src/features/pcp/RenglonDetalle.test.tsx` | Component integration | 10/10 passed | Exact-key assertion failed without `exact: true` | 10/10 passed | Existing two-provider selection exercises the non-trivial mutation path | None needed |

- **Workload / PR boundary:** corrective slice is the exact invalidation option, its focused assertion, and this evidence; the encompassing 4.2 implementation task remains visibly `[ ]` because its dialog, parent wiring, and route are unfinished. No task checkbox changed.

## Work Unit 4 — Renglones 4.2 GREEN (dialog, parent wiring, nested route)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for this bounded second 4.2 GREEN row (17/39 complete going in; `RenglonDetalle.tsx` and `SeleccionProveedoresSection.tsx` were already created in the prior bounded subunit and were read as-is, not modified). Only the permitted edit surfaces (the four target components/route, `PcpDetalle.tsx`, their tests, `routeTree.gen.ts`, `tasks.md`, this record) were touched; no SDD status, attempt, review, or delivery authority command was run. The native ledger token supplied by the orchestrator was not re-acquired because no runtime-bearing SDD command was needed in this slice.
- **Completed / persisted checkbox:** marked the second section 4.2 implementation row visibly `[x]` in `tasks.md` after the focused component and build verification passed.
- **Files changed:** created `frontend/src/features/pcp/CrearRenglonDialog.tsx`, `CrearRenglonDialog.test.tsx`, and `frontend/src/routes/_authenticated.pcp.$pcpId.renglones.$renglonId.tsx`; modified `frontend/src/features/pcp/PcpDetalle.tsx` and `PcpDetalle.test.tsx`; regenerated `frontend/src/routeTree.gen.ts` via the TanStack Router Vite plugin. `RenglonDetalle.tsx` and `SeleccionProveedoresSection.tsx` were read only, not edited.
- **Behavior:** `CrearRenglonDialog` is a self-contained Radix dialog (mirrors `CrearPcpDialog`/`AgregarProveedorProductoDialog`) that submits only `{item_proceso_id}` via `crearRenglon`, renders the normalized 422 message inline via `role="alert"` without closing, and on success invalidates exactly `pcpQueryKeys.renglones(pcpId)` (not the aggregate `pcp` list, not the selection key) before closing and resetting its field. `PcpDetalle.tsx` now mounts `CrearRenglonDialog` next to the "Renglones" heading only when `puedeRol(perfil?.rol, PCP_WRITE_ROLES)` is true; the pre-existing renglón row anchors (`/pcp/{pcpId}/renglones/{renglon.id}`) and the Negociado/Pendiente badge were left untouched, since they already satisfied the detail-navigation and read-only-rendering requirements. The new nested route repeats the `requireRole(...PCP_READ_ROLES)` guard from `_authenticated.pcp.$pcpId.tsx` and mounts `RenglonDetalle` with both merged path params.
- **Verification:** RED before production changes — `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts src/features/pcp/CrearRenglonDialog.test.tsx` → **2 test files failed / 2 passed** (`CrearRenglonDialog.test.tsx` failed to resolve the missing module; `PcpDetalle.test.tsx` failed 1 new test on the missing "Nuevo renglón" trigger; the two safety-net files passed in full). GREEN/TRIANGULATE — same exact command → **4 files / 20 tests passed**. `cd frontend && npm run build` → `tsc -b && vite build` passed after `npx vite build` regenerated `routeTree.gen.ts` (the plugin only rewrites the route tree during a Vite build/dev pass, and `tsc -b` type-checks against it first, so the plain `npx vite build` regeneration step had to run once before the full `npm run build` could type-check the new route); confirmed the new route entry `/_authenticated/pcp/$pcpId/renglones/$renglonId` and its own chunk `_authenticated.pcp._pcpId.renglones._renglonId-*.js` in the build output. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), reporting only the same pre-existing CRLF conversion warnings on unrelated tracked files (`.atl/skill-registry.md`, `.gitignore`, `routeTree.gen.ts`, the backend negotiation module and its tests).

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 4.2 second row — dialog, PcpDetalle wiring, nested route | `frontend/src/features/pcp/CrearRenglonDialog.test.tsx`, `frontend/src/features/pcp/PcpDetalle.test.tsx` | Component integration | `RenglonDetalle.test.tsx` 3/3 and `pcp.test.ts` 7/7 passed unchanged | Dialog suite failed to resolve missing module; PcpDetalle suite failed 1/8 on missing trigger | 4 files / 20 tests passed | Distinct successful-creation (exact targeted invalidation) and server-validation-error (dialog stays open) cases in `CrearRenglonDialog.test.tsx`, plus separate write-role-visible and read-role-hidden cases in `PcpDetalle.test.tsx` | None needed; the dialog mirrors the existing `CrearPcpDialog`/`AgregarProveedorProductoDialog` shape exactly |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts src/features/pcp/CrearRenglonDialog.test.tsx` → 4 files / 20 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` → `tsc -b && vite build` passed; new nested route confirmed present in `routeTree.gen.ts` and produced its own build chunk |
| Rollback boundary | `CrearRenglonDialog.tsx`, `CrearRenglonDialog.test.tsx`, the new nested route file, the `CrearRenglonDialog` mount/import in `PcpDetalle.tsx`, the added `PcpDetalle.test.tsx` cases, and the regenerated `routeTree.gen.ts` entry only — `RenglonDetalle.tsx`, `SeleccionProveedoresSection.tsx`, and the rest of `PcpDetalle.tsx` are unaffected |

- **Workload / PR boundary:** bounded second 4.2 GREEN slice only; authored source/test delta is **50 (CrearRenglonDialog.tsx) + 51 (CrearRenglonDialog.test.tsx) + 12 (nested route) + 4 (PcpDetalle.tsx net) + 24 (PcpDetalle.test.tsx net) = 141 changed lines**, well below the 400-line budget; the generated `routeTree.gen.ts` diff (+126 lines) is excluded from authored risk count per the review-budget guard. Rollback boundary is exactly the files listed above. No commit, push, PR, or later task (4.3 or Work Unit 5) was started.

## Work Unit 7 — Sugerencias 7.1 RED

- **Status / safety:** consumed the orchestrator-injected `pcp-frontend` OpenSpec context for the bounded 7.1 RED-only subunit (29/39 complete going in), token `sha256:26dcc652af1c131915ac7c8ca6e08ebd73877b1553a7284778f5629270a49872`, and the sole allowed edit root `C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE`. No SDD status, attempt, review, or delivery authority command was run beyond the required acquire/settle ledger calls.
- **Completed / persisted checkbox:** marked section 7.1 RED visibly `[x]` in `tasks.md` after confirming the intended-failure kind. No production component, API method, or route was added — this subunit is RED-only by design.
- **Files changed:** `frontend/src/features/pcp/SugerenciasPanel.test.tsx` (new), `tasks.md`, and this record. `services/pcp/sugerencias/**` was read only, not touched.
- **RED evidence:** the new test file mocks `@/lib/api/pcp` with two not-yet-existing named exports, `obtenerSugerenciaAgrupacion(renglonId)` (mirrors backend `GET /pcp/sugerencias/renglones/{renglonId}/agrupacion`, returns `SugerenciaAgrupacionOut | null`) and `listarSugerenciasPreciosRecientes(renglonId)` (mirrors `GET /pcp/sugerencias/renglones/{renglonId}/precios-recientes`, returns `SugerenciaPrecioRecienteOut[]`), and imports the not-yet-existing `SugerenciasPanel` component. Five cases were written: (1) quantity-grouping suggestion shows the aggregated `cantidad_agregada` and `producto_id`; (2) recent-price suggestion shows `proveedor`, `mantenimiento_hasta`, `cantidad_minima`/`cantidad_maxima`, and `precio_unitario`; (3) no-data state (`null` + `[]`) shows an explicit empty message and neither suggestion heading; (4) `it.each` over all four `PCP_READ_ROLES` (`superadmin`, `admin`, `gerencia`, `compras`) proves the panel renders as an accessible `role="region"` for every read role, with no write-role gate in the test; (5) with both suggestion types populated, asserts zero `role="button"` and zero `role="checkbox"` elements exist in the panel.
- **Verification:** safety net `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **9 files / 82 tests passed**, with `SugerenciasPanel.test.tsx` failing to resolve as the 10th file (expected, pre-existing at that point in the run). Required focused command `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx` → **1 file failed, 0 tests run**, with the exact Vite error `Failed to resolve import "./SugerenciasPanel" from "src/features/pcp/SugerenciasPanel.test.tsx". Does the file exist?` — a missing-module resolution failure, not a syntax error inside the test file itself, confirming this is the correct kind of RED. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → exit 0, reporting only the same pre-existing CRLF conversion warnings on unrelated tracked files already recorded in prior slices.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 7.1 RED suggestions panel | `frontend/src/features/pcp/SugerenciasPanel.test.tsx` | Component integration | 9 files / 82 tests passed | Failed to resolve missing `./SugerenciasPanel` module (0 tests ran) | Not run — RED-only assigned subunit | Grouping display, recent-price display, no-data state, four-role visibility loop, and mutation-control-absence cases are all written and distinct | None — no production code in this subunit |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx` → 1 file failed / 0 tests (missing-module RED, as required) |
| Runtime harness command/scenario and exact result | N/A — RED-only subunit adds no production code or runtime boundary; `npm run build` is deferred to the 7.2/7.3 GREEN and refactor rows |
| Rollback boundary | `frontend/src/features/pcp/SugerenciasPanel.test.tsx` and this progress entry only |

- **Workload / PR boundary:** bounded RED-only Sugerencias slice; the new test file is 101 lines, well under the 400-line budget. Rollback boundary is only the new test file plus the `tasks.md`/progress records. No commit, push, PR, or later task was started.
- **Design deviations / action-context warnings:** none. The two mocked API function names (`obtenerSugerenciaAgrupacion`, `listarSugerenciasPreciosRecientes`) are new naming decisions for this subunit — design.md's `lib/api/pcp.ts` Contract list does not enumerate sugerencias methods explicitly, so these follow the established `obtenerX` (nullable/singular)/`listarX` (array) convention from the rest of the file; the 7.2 GREEN row must implement exactly these two names to turn this RED green.
- **Remaining implementation tasks:** the next exact unchecked row is: `- [ ] Add typed grouping and recent-price reads to \`frontend/src/lib/api/pcp.ts\`, implement \`frontend/src/features/pcp/SugerenciasPanel.tsx\`, and mount it in \`frontend/src/features/pcp/RenglonDetalle.tsx\` with its dedicated query keys and loading/error/empty states; verify the suggestions RED cases pass; rollback the panel and its API/detail integrations. <!-- sdd-owner: implementation -->` All later 7.3, section 8, and section 9 rows remain unchecked in `tasks.md`.
- **Design deviations / action-context warnings:** none. The renglón create form exposes only `item_proceso_id` (a plain text field for the process-item id) because no `origen`/`regla_pcp_id` UI affordance or process-item lookup client exists yet in this change's scope, and the spec's create-renglón scenario only requires a valid `item_proceso_id` to succeed; `crearRenglon` already omits both optional keys unless explicitly set, so this does not weaken the `extra=forbid` contract already covered by `pcp.test.ts`.
- **Remaining implementation tasks:** the next exact unchecked row is section 4.3's first item: `- [ ] Add renglón tests for zero catalogued suppliers, partial multi-selection, and server-validation errors rendered inline without silently dropping fields; verify with the focused renglón/API Vitest command; rollback the added edge-case tests. <!-- sdd-owner: implementation -->`. All later Negociación through cross-unit validation rows remain unchecked in `tasks.md`.

## Work Unit 4 — Renglones 4.3 TRIANGULATE and REFACTOR

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the bounded section-4.3 objective (19/39 complete going in). Only the four named files (`frontend/src/features/pcp/RenglonDetalle.test.tsx`, `frontend/src/lib/api/pcp.ts`, `frontend/src/lib/api/pcp.test.ts`, `tasks.md`) plus this record were touched; `PcpDetalle.tsx`, `CrearRenglonDialog.tsx`, `SeleccionProveedoresSection.tsx`, and `queryKeys.ts` were read as-is and not modified. No SDD status, attempt, review, or delivery authority command was run; the supplied native ledger token was not re-acquired since no runtime-bearing SDD command was needed.
- **Completed / persisted checkboxes:** marked both section 4.3 rows visibly `[x]` in `tasks.md` (21/39 complete).
- **TRIANGULATE — files changed and behavior:** added three `RenglonDetalle.test.tsx` cases (zero catalogued suppliers renders the existing empty-state text with no list/checkbox/confirm control; selecting only one of three catalogued suppliers submits exactly `['prov-2']`, never the unselected pair; a 422-shaped selection error renders inline via the existing `role="alert"` message while both previously checked suppliers remain checked, proving `SeleccionProveedoresSection`'s `proveedorIds` state is never cleared on error) and one `pcp.test.ts` case proving `seleccionarProveedores` normalizes a FastAPI 422 array `detail` into `"proveedor_ids: Field required"`, mirroring the existing `crearRenglon` contract. Only the last API-layer case required a production change: `seleccionarProveedores` in `pcp.ts` now awaits `presupuestacionFetch` inside a `try/catch` and rethrows through the existing (already-exported-internally) `normalizarErrorValidacion` helper, exactly like `crearRenglon`. The two UI-layer approval cases (empty state, partial subset) and the UI half of the inline-error case passed immediately against the pre-existing `SeleccionProveedoresSection`/`RenglonDetalle` implementation from Work Unit 4.2 — no component change was needed or made, confirmed explicitly rather than inventing one.
- **REFACTOR — investigation and explicit no-op finding:** read `PcpDetalle.tsx`, its route (`_authenticated.pcp.$pcpId.tsx`), `RenglonDetalle.tsx`, and the nested route (`_authenticated.pcp.$pcpId.renglones.$renglonId.tsx`). `PcpDetalle` already calls only `obtenerPcp`, `listarRenglones`, and `listarRenglonesSeleccionados` (PCP-level summary/list/badge data); `RenglonDetalle` is the sole caller of `obtenerDetalleRenglon` (enriched product + catalogued-suppliers data), reached only through its own nested route component reading `Route.useParams()` directly — confirmed via a repo-wide search that no other file references `obtenerDetalleRenglon`, `RenglonDetalle`, or `proveedores_catalogados`. There is no duplicate fetching and no prop-drilling of enriched renglón data from the parent. This separation was already correct from the prior 4.2 slice; **no refactor code change was made** because none was needed, per the explicit instruction to state this rather than invent an unnecessary change.
- **Verification:** safety net `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/CrearRenglonDialog.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts` → **4 files / 20 tests passed**. RED (API-layer only): `cd frontend && npm run test -- src/lib/api/pcp.test.ts` after adding the new `seleccionarProveedores` 422 case → **1 file / 1 failed, 7 passed** (`MockApiError{status:422}` lacked the normalized `message`). GREEN: same command → **1 file / 8 tests passed**. Required focused command after all four new tests: `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/CrearRenglonDialog.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts` → **4 files / 24 tests passed**. `cd frontend && npm run build` → `tsc -b && vite build` passed. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), only the same pre-existing CRLF conversion warnings on unrelated tracked files.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 4.3 TRIANGULATE — renglón edge cases | `frontend/src/features/pcp/RenglonDetalle.test.tsx`, `frontend/src/lib/api/pcp.test.ts` | Component integration + API contract | 4 files / 20 tests passed | 1 new API case failed on missing 422 normalization in `seleccionarProveedores`; the 3 new component cases passed immediately as approval coverage of existing 4.2 behavior | 4 files / 24 tests passed | Zero-supplier empty state, 1-of-3 partial subset, and inline 422-with-preserved-selection are three distinct code paths beyond the prior all-selected/none-selected cases | None needed; `seleccionarProveedores` reuses the existing `normalizarErrorValidacion` helper unchanged |
| 4.3 REFACTOR — renglón query ownership | N/A (read-only investigation) | N/A | N/A | N/A | N/A | N/A | Confirmed already compliant: no code changed, explicitly reported rather than inventing a refactor |

## Work Unit 6 — Consultas 6.1 RED

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the bounded section-6.1 RED-only row (29/39 complete going in, since sections 1–5 are all `[x]`). The permitted edit surfaces (`frontend/src/features/pcp/ConsultaDetalle.test.tsx`, `frontend/src/lib/api/pcp.test.ts`, `tasks.md`, this record) were honored; no production component, route, or API method was added. No SDD status, attempt, review, or delivery authority command was run.
- **Completed / persisted checkbox:** marked section 6.1 RED visibly `[x]` in `tasks.md` (30/39 complete) after confirming the RED failure was the correct kind.
- **Files changed:** created `frontend/src/features/pcp/ConsultaDetalle.test.tsx`; extended `frontend/src/lib/api/pcp.test.ts` with a `@/lib/supabase` mock, typed accessors for the four not-yet-existing consultation client methods, five new JSON-contract test cases, and a new `descargarPdfConsulta (descarga binaria)` describe block with two blob-fetch cases.
- **Test contract established (RED, no production code):**
  - `agruparConsultas({selecciones, contacto_id?, fecha_respuesta_esperada?, canal?})` → `POST /pcp/consultas` with the exact `{selecciones}` body (mirrors backend `AgruparConsultaCreate`/`SeleccionParaAgrupar` from `services/pcp/consultas/models.py`), returning one `Consulta` per distinct `proveedor_id` present in `selecciones` (server-side grouping, confirmed by reading `services/pcp/consultas/service.py::agrupar_renglones`).
  - `obtenerConsulta(id)` → `GET /pcp/consultas/{id}`; `enviarConsulta(id)` → `POST /pcp/consultas/{id}/enviar`.
  - `descargarPdfConsulta(id)` cannot reuse `presupuestacionFetch` (design.md, `lib/api/pcp.ts` Contract section): it duplicates the auth-header fetch against `GET /pcp/consultas/{id}/pdf` using the session access token and returns `response.blob()` directly, never calling `.json()`. Tested by mocking `@/lib/supabase`'s `getSession` and stubbing global `fetch` (no existing binary-fetch convention was found elsewhere in the frontend to reuse; `presupuestacion.ts` only exposes the JSON-parsing `presupuestacionFetch`).
  - `ConsultaDetalle({consultaId})` renders an existing consultation's proveedor/estado (no renglón-listing method exists in the backend contract — `GET /pcp/consultas/{id}` returns only `ConsultaOut`, confirmed by reading `services/pcp/consultas/router.py`; no `listar_renglones_consulta` endpoint is exposed), a "Descargar PDF" action calling `descargarPdfConsulta` without rendering raw blob content, and an "Enviar consulta" action visible only for `PCP_WRITE_ROLES` (computed via `useAuth`, matching `RenglonDetalle`'s convention) that shows a plain "Consulta enviada" confirmation on success with no messaging-adapter/channel text, and an inline `role="alert"` error on failure while the button remains enabled for retry.
  - `AgruparConsultaDialog({selecciones, puedeEscribir})` is a standalone dialog taking an explicit `puedeEscribir` prop (a deliberate deviation from the `CrearRenglonDialog`/`CerrarPcpDialog` precedent, documented below) that calls `agruparConsultas({selecciones})` on confirm and renders no trigger at all when `puedeEscribir` is false.
- **Design deviation (explicitly noted, not silent):** every other PCP dialog (`CrearRenglonDialog`, `CerrarPcpDialog`, `RegistrarResultadoDialog`) is mounted conditionally by its parent (`{puedeEscribir ? <Dialog /> : null}`) and has no internal role awareness. `AgruparConsultaDialog` instead takes an explicit `puedeEscribir` prop, matching the `ComparacionProveedoresTable`/`SeleccionProveedoresSection` precedent instead, because: (a) design.md's task 6.2 row states the grouping entry must be exposed "only from eligible PCP/renglón flow" — i.e., from two distinct parent contexts (`PcpDetalle` and/or `RenglonDetalle`), not one; (b) this RED subunit's file scope is restricted to `ConsultaDetalle.test.tsx` and `pcp.test.ts` only, so a role-gating test could not be written against either actual mount site without touching `PcpDetalle.tsx`/`RenglonDetalle.tsx`, which are outside this bounded row; (c) an explicit prop lets both future mount sites reuse one role check instead of duplicating it, and keeps this RED-established contract independently testable in isolation now. The launch prompt's own phrasing — "likely from an `AgruparConsultaDialog` trigger mocked/stubbed at this layer if it's a separate component" — was read as permission to test the component directly in this file rather than through a not-yet-built parent. The "grouping selected renglones into one consultation per provider" test case is additionally covered at the API-contract layer in `pcp.test.ts` (one `agruparConsultas` call, multiple returned consultations, one per distinct provider).
- **Verification:** safety net `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` (run after the new files existed, since this was a corrective ordering — see Issues below) → **9 files total: 7 passed unchanged, 2 failed** (`ConsultaDetalle.test.tsx` and `pcp.test.ts`); exactly **61 pre-existing tests still passed** across the 7 unaffected files, confirming no regression. Required focused RED command `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/lib/api/pcp.test.ts` → **2 test files failed (2), 5 tests failed / 14 passed (19)**. `ConsultaDetalle.test.tsx` failed with `Error: Failed to resolve import "./ConsultaDetalle" from "src/features/pcp/ConsultaDetalle.test.tsx". Does the file exist?` (module-resolution failure, not a syntax error in the test itself — the import chain also references the equally-missing `./AgruparConsultaDialog`). The 5 failing `pcp.test.ts` cases each failed with `TypeError: <method> is not a function` for `agruparConsultas`, `obtenerConsulta`, `enviarConsulta`, and `descargarPdfConsulta` (twice) — none of which exist in `pcp.ts` yet; the 14 pre-existing `pcp.test.ts` cases passed unchanged. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), reporting only the same pre-existing CRLF conversion warnings on unrelated tracked files already logged in prior work units.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 6.1 RED — consultation grouping/detail/PDF/send client and view | `frontend/src/features/pcp/ConsultaDetalle.test.tsx`, `frontend/src/lib/api/pcp.test.ts` | Component integration + API contract | 7 unaffected files / 61 tests passed unchanged | Focused suite fails: missing `./ConsultaDetalle` (and `./AgruparConsultaDialog`) modules; 5 missing client methods on `pcpClient` | Not run — RED-only assigned subunit | Grouping (multi-provider), detail render, PDF download, write/read send visibility, send success, and send failure-keeps-usable are seven distinct cases across the two files | None — no production code in this subunit |

- **Workload / PR boundary:** bounded RED-only Consultas slice; rollback is only `ConsultaDetalle.test.tsx`, the `pcp.test.ts` test additions, and these task/progress records. No commit, PR, delivery action, or later task (6.2 GREEN) was started.

## Work Unit 7 — Sugerencias 7.2 GREEN

- **Status / safety:** consumed the orchestrator-injected `pcp-frontend` OpenSpec apply-ready context for the bounded 7.2 GREEN row (30/39 complete going in). The permitted edit surfaces (`frontend/src/lib/api/pcp.ts`, `frontend/src/features/pcp/SugerenciasPanel.tsx`, `frontend/src/features/pcp/RenglonDetalle.tsx`, `frontend/src/features/pcp/RenglonDetalle.test.tsx`, `tasks.md`, this record) were honored. Ran the native ledger acquire (`work-unit: sugerencias-7.2-green`) and settle exactly as instructed; `sdd-attempt status` confirmed no `active_attempt` block remains and the last attempt row (`sugerencias-7.2-green`) shows `outcome: passed`.
- **Completed / persisted checkbox:** marked section 7.2 GREEN visibly `[x]` in `tasks.md` (31/39 complete, confirmed with `grep -c "^- \[x\]" tasks.md`).
- **Files changed:** `frontend/src/lib/api/pcp.ts` (added `SugerenciaAgrupacion`/`SugerenciaPrecioReciente` types and `obtenerSugerenciaAgrupacion`/`listarSugerenciasPreciosRecientes` reads against the exact backend paths confirmed by reading `services/pcp/sugerencias/router.py`: `GET /pcp/sugerencias/renglones/{renglonId}/agrupacion` and `.../precios-recientes`); created `frontend/src/features/pcp/SugerenciasPanel.tsx`; mounted `<SugerenciasPanel renglonId={renglonId} />` in `frontend/src/features/pcp/RenglonDetalle.tsx` right after `ComparacionProveedoresTable`; extended `RenglonDetalle.test.tsx`'s pre-existing `@/lib/api/pcp` mock/`beforeEach` with the two new functions (resolved `null`/`[]`) so its unrelated validation-error assertion (`findByRole('alert')`) would not collide with the panel's own error-state alert once real API calls started resolving through the mocked module.
- **Behavior:** `SugerenciasPanel` uses two independent `useQuery` calls keyed by the pre-existing `pcpQueryKeys.agrupacion(renglonId)`/`pcpQueryKeys.preciosRecientes(renglonId)`, with explicit loading (`Cargando sugerencias…`) and error (`role="alert"`) states, and an explicit `role="region" aria-label="Sugerencias del renglón"` wrapper rendered in every state so the panel is discoverable regardless of role — no `puedeEscribir`/write-role gate exists anywhere in the component, matching the spec's "visible to every read role" requirement. The quantity-grouping suggestion (nullable) and the recent-price list are each conditionally rendered only when present; when both are absent, an explicit `No hay sugerencias disponibles para este renglón.` message renders instead. No button, checkbox, or other interactive/mutating control exists in the component.
- **Verification:** safety net before edits `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **9 files / 82 tests passed**, `SugerenciasPanel.test.tsx` failing to resolve as the 10th file (expected pre-existing RED, matching the prior recorded state). Focused required command `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx` → **1 file / 8 tests passed** (fully GREEN, frozen test file untouched). `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/SugerenciasPanel.test.tsx` → first run surfaced 1 failure in `RenglonDetalle.test.tsx`'s pre-existing 422-validation-alert case (`findByRole('alert')` resolved to the panel's own `"No se pudieron cargar las sugerencias."` alert instead, because the file's `@/lib/api/pcp` mock factory did not include the two new functions, so they were `undefined` and threw inside the panel's `queryFn`s); after extending that mock and its `beforeEach`, the same command → **2 files / 15 tests passed**. Full PCP suite `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **10 files / 90 tests passed**. `cd frontend && npm run build` → `tsc -b && vite build` passed, with a new `_authenticated.pcp._pcpId.renglones._renglonId-*.js` chunk reflecting the panel addition. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → exit 0, only the same pre-existing CRLF conversion warnings on unrelated tracked files already logged in prior work units.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 7.2 GREEN suggestions panel | `frontend/src/features/pcp/SugerenciasPanel.test.tsx`, `frontend/src/features/pcp/RenglonDetalle.test.tsx` | Component integration + API contract | 9 files / 82 tests passed | Frozen `SugerenciasPanel.test.tsx` failed to resolve missing module (0 tests ran, pre-existing from 7.1) | `SugerenciasPanel.test.tsx` alone: 8/8 passed | The five frozen cases (grouping display, recent-price display, no-data empty state, four-role visibility loop, mutation-control absence) already exercise distinct code paths — no additional triangulation case was added since this was a GREEN-only assigned subunit against an already-comprehensive frozen RED suite | None needed; component boundaries (two independent `useQuery` calls, shared loading/error wrapper) kept the implementation minimal and readable on first pass |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx` → 1 file / 8 tests passed; `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/SugerenciasPanel.test.tsx` → 2 files / 15 tests passed; `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → 10 files / 90 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` → `tsc -b && vite build` passed; the existing renglón nested-route chunk grew to include the mounted panel, confirming it is reachable through the real route tree, not only the isolated component test |
| Rollback boundary | `frontend/src/lib/api/pcp.ts` (only the new sugerencias types/functions), `frontend/src/features/pcp/SugerenciasPanel.tsx` (new file), the one-line mount + import in `frontend/src/features/pcp/RenglonDetalle.tsx`, the two-function mock/`beforeEach` addition in `frontend/src/features/pcp/RenglonDetalle.test.tsx`, and this progress entry only — `SugerenciasPanel.test.tsx` itself was not modified |

- **Workload / PR boundary:** bounded 7.2 GREEN slice only; authored source/test delta is **+83 (pcp.ts) + 79 (SugerenciasPanel.tsx new) + 2 (RenglonDetalle.tsx net) + 7 (RenglonDetalle.test.tsx net) = ~171 changed lines**, well below the 400-line budget. Rollback boundary is exactly the files listed above. No commit, push, PR, or later task (7.3) was started.
- **Design deviations / action-context warnings:** none. Confirmed both backend endpoint paths and response shapes by reading `services/pcp/sugerencias/router.py` and `models.py` directly rather than trusting the 7.1 author's note at face value, per instruction; they matched exactly. The RenglonDetalle.test.tsx mock extension was a necessary compatibility fix for a pre-existing sibling test file, not a deviation from design — the frozen `SugerenciasPanel.test.tsx` itself was not touched.
- **Remaining implementation tasks:** the next exact unchecked rows are section 7.3: `- [ ] Add suggestions tests for a valid recent-price row and multiple open-renglón quantity aggregation, then run focused Vitest plus \`cd frontend && npm run build\`; rollback the added suggestions cases. <!-- sdd-owner: implementation -->` and `- [ ] Refactor suggestions rendering to remain read-only and isolated from renglón mutations; verify the focused suggestions test command; rollback the suggestions-only refactor. <!-- sdd-owner: implementation -->`. All of section 8 (Legacy imports) and section 9 (Cross-unit validation) remain unchecked in `tasks.md`.
- **Issues found:** the safety-net command was run once after the new test files already existed rather than strictly before, because the assigned two-file scope for this row is itself entirely new/additive (no pre-existing `ConsultaDetalle.test.tsx` to baseline, and `pcp.test.ts`'s pre-existing 14 cases are visible unaffected in both runs regardless of order). No production or unrelated test file was touched, so this does not change the correctness of the reported baseline (61/61 unaffected tests both before and after, confirmed by the identical count in the required focused command's "14 passed" segment matching the pre-existing count exactly).
- **Remaining implementation tasks:** the next exact unchecked row is section 6.2's implementation item: `- [ ] Add typed consultation grouping/detail/send methods and the auth-header blob download implementation to \`frontend/src/lib/api/pcp.ts\`, then implement \`frontend/src/features/pcp/AgruparConsultaDialog.tsx\`, \`ConsultaDetalle.tsx\`, and \`frontend/src/routes/_authenticated.pcp.consultas.$consultaId.tsx\`; expose the grouping entry only from eligible PCP/renglón flow and target cache updates to consultation keys; verify consultation RED cases pass; rollback consultation components, route, and API additions. <!-- sdd-owner: implementation -->`. All later Consultas TRIANGULATE/REFACTOR, Sugerencias, Legacy imports, and cross-unit validation rows remain unchecked in `tasks.md`.

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/CrearRenglonDialog.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts` → 4 files / 24 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` → `tsc -b && vite build` passed |
| Rollback boundary | `seleccionarProveedores`'s `try/catch` in `frontend/src/lib/api/pcp.ts`, the new `pcp.test.ts` 422 case, the three new `RenglonDetalle.test.tsx` cases, and this progress/task-checkbox update only — no other production component was touched |

- **Workload / PR boundary:** bounded 4.3-only slice; authored delta is small (`pcp.ts` +10/-6 = 16 changed lines; `pcp.test.ts` +16/-1 = 17 changed lines; `RenglonDetalle.test.tsx` +37/-0 = 37 changed lines), roughly **70 changed lines**, well below the 400-line budget and the 400-line objective cap given to this run. Rollback boundary is exactly the three files above. No commit, push, PR, or later work unit (Negociación, section 5+) was started.
- **Design deviations / action-context warnings:** none. The REFACTOR row's finding (already-compliant separation) is a confirmation, not a deviation.
- **Remaining implementation tasks:** section 4 (Renglones) is now fully `[x]`. The next exact unchecked row is section 5.1: `- [ ] Create failing \`frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx\` cases for loading, no catalogued providers, per-provider 404-as-no-result, \`precio_obtenido\`, \`no_cotiza\`, desktop columns, mobile cards, absent read-only mutations, no close control, and two simultaneous selected providers; verify with \`cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx\`; rollback the new comparison test file. <!-- sdd-owner: implementation -->`. Work Unit 5 (Negociación) was intentionally not started per this run's bounded scope.

## Work Unit 5 — Negociación 5.1 RED

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the bounded section-5.1 RED-only objective (21/39 complete going in), native ledger token `sha256:d7baa02f58afcd3f77fcee04173c36bccd8db5b6b692ec2a7afb38f7f1d80a55`. The workspace root was the sole allowed edit root. No SDD status, review, or delivery authority command was run beyond the acquired attempt token.
- **Completed / persisted checkbox:** marked section 5.1 visibly `[x]` in `tasks.md` (22/39 complete) after confirming the RED failure is the expected missing-module kind.
- **Files changed:** created `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx` only. No production component, API method, or route was added, per the RED-only scope.
- **Test contract established (drives the next 5.2 GREEN slice):** the test file mocks `@/lib/api/pcp`'s not-yet-existing `obtenerResultado`/`actualizarSeleccion` and mocks `@/lib/api/presupuestacion`'s `ApiError` (same `MockApiError` shape as `pcp.test.ts`) so a future `instanceof ApiError && status === 404` check in the component's `useQueries` fan-out can be exercised. It defines: a `ComparacionProveedoresTable({pcpId, renglonId, proveedores, puedeEscribir})` component; a "Cargando comparación…" loading text; "No hay proveedores catalogados para este renglón." empty state; a per-provider "Sin resultado aún" text when `obtenerResultado` rejects with a 404 `ApiError` (bounded fan-out asserted via `toHaveBeenCalledTimes(2)` with exact `(pcpId, renglonId, proveedorId)` args, never a batched call); "Precio obtenido" / "No cotiza" result labels (the latter alongside `motivo`, never a price); a desktop `<table aria-label="Comparación de proveedores">` with one `columnheader` per proveedor code and a `rowheader` named "Resultado"; a mobile `<ul aria-label="Comparación de proveedores en tarjetas">` of `listitem` cards carrying the same proveedor code and resultado text; read-only role (`puedeEscribir=false`) hides any button matching `/resultado/i` or `/selecci/i`; no button or text matching `/cerrar pcp/i` is ever rendered, even for `puedeEscribir=true`; and two providers with `seleccionado: true` both render the "Seleccionado" tag and a "Quitar selección" button simultaneously (non-exclusive).
- **Verification:** safety net `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx` → **2 files / 14 tests passed**, unchanged. RED `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx` → **1 failed suite / 0 tests run**, failing with `Error: Failed to resolve import "./ComparacionProveedoresTable" ... Does the file exist?` — the required missing-module RED, not a syntax error in the new test file. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), reporting only the same pre-existing CRLF conversion warnings on unrelated tracked files already recorded in prior entries.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 5.1 RED — comparison table contract | `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx` | Component integration | `pcp.test.ts` + `RenglonDetalle.test.tsx`: 2 files / 14 tests passed | Whole suite fails to resolve the missing `ComparacionProveedoresTable` module (module-not-found, not a test assertion failure) | Not run — RED-only assigned subunit; no production code was written | 10 distinct cases (loading, empty, 404-as-no-result with bounded call-count assertion, precio_obtenido, no_cotiza+motivo, desktop columns, mobile cards, read-only absence, no-close-control, two-simultaneous-selected) define the acceptance surface for 5.2 GREEN | None — no production code in this subunit |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx` → 1 failed suite (module-not-found), 0 tests run — the required RED |
| Runtime harness command/scenario and exact result | N/A — RED-only test-authoring subunit; no production/build surface exists yet to exercise (`npm run build` is deferred to the 5.2/5.3 GREEN and REFACTOR slices per the work-unit boundary) |
| Rollback boundary | `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx`, the section-5.1 checkbox in `tasks.md`, and this progress entry only |

- **Workload / PR boundary:** bounded RED-only Negociación slice; the new test file is 175 added lines, well below the 400-line budget. Rollback boundary is exactly the one new test file plus the two record updates above. No commit, push, PR, or later task (5.2 GREEN onward) was started.
- **Design deviations / action-context warnings:** none. The exact wording of the "no result yet"/"resultado" labels and the desktop/mobile markup shape (table + `role="list"` card duplication, both always present in the DOM regardless of CSS breakpoint, consistent with jsdom having no layout) are test-authored contract choices for the not-yet-written component; they are not deviations from design.md, which left exact markup unspecified beyond "column per proveedor, row per criterion" and "stacked card list below `md`".
- **Remaining implementation tasks:** the next exact unchecked row is section 5.2's first item: `- [ ] Extend \`frontend/src/lib/api/pcp.ts\` with typed result GET/POST and \`actualizarSeleccion\` PATCH methods, then implement \`frontend/src/features/pcp/ComparacionProveedoresTable.tsx\` and \`RegistrarResultadoDialog.tsx\` using a bounded \`useQueries\` fan-out over catalogued providers and treating only \`ApiError.status === 404\` as no result; verify comparison RED cases pass; rollback the negotiation components and API additions. <!-- sdd-owner: implementation -->`. All later Negociación TRIANGULATE/REFACTOR rows through Legacy imports and cross-unit validation remain unchecked in `tasks.md`.

## Work Unit 5 — Negociación 5.2 GREEN (API/backend slice only)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (22/39 complete going in), native ledger token `sha256:1a3345bd046ae92f5008656348e3a3bb13ddf8e82aee5e696a2db12fb78fd978` (acquired with `--request-id pcp-frontend-5-2-green-api-backend-join`). The workspace root was the sole allowed edit root. This is the API/backend half of the first section-5.2 row only; `ComparacionProveedoresTable.tsx` and `RegistrarResultadoDialog.tsx` are intentionally NOT created in this objective.
- **Completed / persisted checkbox:** the first section-5.2 row remains visibly `- [ ]` in `tasks.md` on purpose — only its API/backend half is done here; the component half (`ComparacionProveedoresTable.tsx`, `RegistrarResultadoDialog.tsx`) is still pending, so the row cannot be marked `[x]` yet per this objective's explicit instruction.
- **Files changed:** `frontend/src/lib/api/pcp.ts`, `frontend/src/lib/api/pcp.test.ts`, `services/pcp/negociacion/models.py`, `services/pcp/negociacion/repository.py`, `services/pcp/negociacion/service.py`, `tests/pcp/negociacion/test_service.py`, `tests/pcp/negociacion/test_router.py`, and this record.
- **Behavior — frontend typed client:** added `obtenerResultado(pcpId, renglonId, proveedorId)` (plain GET, never catches a 404 — the caller/`ComparacionProveedoresTable` decides what "no result yet" means, matching the D4 fan-out contract already established by the 5.1 RED test), `registrarResultado(pcpId, renglonId, proveedorId, payload)` (explicit allowlist body construction identical in spirit to `crearRenglon`/`seleccionarProveedores` — starts from `{resultado}` and conditionally adds each of the seven optional keys only when defined, never spreads the caller's payload object; reuses the existing `normalizarErrorValidacion` 422 helper), and `actualizarSeleccion(pcpId, renglonId, proveedorId, seleccionado)` (PATCH sending exactly `{seleccionado}`). Extended the `ResultadoNegociacion` interface with the six new backend fields (below) and added a `RegistrarResultadoPayload` type.
- **Behavior — pre-approved backend read-model join (Design deviation, additive, orchestrator pre-approved — not a `tasks.md` row):** `services/pcp/negociacion/models.py::ResultadoNegociacionOut` gained `precio_unitario`, `cantidad_minima`, `cantidad_maxima`, `mantenimiento_hasta`, `condicion_pago_id`, `forma_pago_id` (all `| None = None`). Added `repository.py::obtener_precio_proveedor(client, *, precio_proveedor_id)` (a plain `select("*").eq("id", ...)` read on `precios_proveedor`, mirroring `buscar_resultado`'s shape). Added `service.py::_con_datos_precio_proveedor(client, resultado)`, a pure enrichment helper: when `resultado["precio_proveedor_id"]` is set it joins those six fields from `precios_proveedor`; otherwise (a `no_cotiza` or the initial `sin_respuesta` row) all six stay `None`, never fabricated. Wired into both `registrar_resultado`'s return and `obtener_resultado`'s return only — `RegistrarResultadoNegociacion`'s write contract, `precios_proveedor` schema, and `actualizar_seleccion`'s return were deliberately left untouched (out of this objective's scope; `actualizar_seleccion`'s `ResultadoNegociacionOut` response will still read `None` for these six fields even on an already-priced row — noted as a risk below, not fixed here since the instruction explicitly scoped this fix to `obtener_resultado`/`registrar_resultado`).
- **Verification (backend):** safety net `./venv/Scripts/python.exe -m pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` → **19 passed** (baseline, unchanged). RED (2 new service tests + 1 new router test, added before any production change) → same command → **3 failed with `KeyError: 'precio_unitario'`** (the two `obtener_resultado` calls and the router `cuerpo["precio_unitario"]` lookup), **19 passed** unchanged. GREEN → same command → **22 passed**, 18 warnings (only the pre-existing `supabase` deprecation warnings).
- **Verification (frontend):** safety net `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx` → **2 files / 14 tests passed** (unchanged). RED (6 new `pcp.test.ts` cases added before any production change) → `cd frontend && npm run test -- src/lib/api/pcp.test.ts` → **6 failed** (`obtenerResultado is not a function`, `registrarResultado is not a function`, `actualizarSeleccion is not a function`), **8 passed** unchanged. GREEN → `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx` → **2 files / 20 tests passed**. Confirmed `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx` still fails, and now **only** on `Error: Failed to resolve import "./ComparacionProveedoresTable"` (module-not-found) — the `@/lib/api/pcp` mock for `obtenerResultado`/`actualizarSeleccion` resolves cleanly, confirming the API half is complete and only the component half remains. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), only the same pre-existing CRLF conversion warnings on unrelated tracked files already recorded in prior entries.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| Backend `ResultadoNegociacionOut` precio/condiciones join | `tests/pcp/negociacion/test_service.py`, `tests/pcp/negociacion/test_router.py` | Service + router integration | 19/19 passed | 3 new cases failed with `KeyError` on the six missing joined fields | 22/22 passed | `precio_obtenido` (all six fields populated and matching what was registered) vs. `no_cotiza` (all six `None`) are the two distinct code paths; a third router-level case proves the same join survives the FastAPI response-model boundary | None needed; `_con_datos_precio_proveedor` is a single small pure-ish helper (one repository read, no branching beyond the `precio_proveedor_id is None` guard already required by the spec) |
| Frontend typed `obtenerResultado`/`registrarResultado`/`actualizarSeleccion` | `frontend/src/lib/api/pcp.test.ts` | API contract | 8/8 prior cases passed (2 files / 14 tests incl. `RenglonDetalle.test.tsx`) | 6 new cases failed on missing client functions | 2 files / 20 tests passed | Exact-PATCH-body, exact-GET-path, 404-propagation (not swallowed), `precio_obtenido` full-optional-body, `no_cotiza` minimal-body, and 422-normalization are six distinct contract cases | None needed; `registrarResultado` reuses the existing `normalizarErrorValidacion` and the same explicit-allowlist-body shape as `crearRenglon` |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | Backend: `./venv/Scripts/python.exe -m pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` → 22 passed. Frontend: `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx` → 2 files / 20 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx` → confirmed still-failing suite, now solely on the missing `ComparacionProveedoresTable` module (no API-mock resolution failure); full `npm run build`/backend coverage run deferred to the next component-wiring slice and cross-unit validation per the work-unit boundary, since no new route or component surface exists yet to build |
| Rollback boundary | Backend: `services/pcp/negociacion/models.py`, `repository.py`, `service.py`, and the three new pytest cases in `test_service.py`/`test_router.py` only — `RegistrarResultadoNegociacion`, `precios_proveedor` schema, and `actualizar_seleccion` untouched. Frontend: the `obtenerResultado`/`registrarResultado`/`actualizarSeleccion` additions and six new interface fields in `pcp.ts`, and the six new `pcp.test.ts` cases only |

- **Workload / PR boundary:** authored delta (additions+deletions, self-counted since `pcp.ts`/`pcp.test.ts` are still untracked from earlier work units so `git diff --numstat` cannot isolate this session's lines on them) is approximately: backend production `models.py` 32 + `repository.py` 15 + `service.py` 30 = **77**; backend tests `test_service.py` 118 + `test_router.py` 63 = **181** (backend total **258**, confirmed independently revertible — the backend prerequisite already has its own rollback boundary per the Work-Unit Boundaries table); frontend `pcp.ts` 100 + `pcp.test.ts` 109 = **209** (also independently revertible, scoped to the typed client only). **Combined total ≈ 467 changed lines, which exceeds this objective's 400-line cap.** Per the apply skill's guidance (implement the assigned slice honestly rather than compressing code/tests to hit a number), this was not trimmed — no comment, blank line, or test was removed to fit the budget. Both halves are self-contained, independently revertible units (backend read-model join vs. frontend typed client) that map onto the already-planned "Backend prerequisite" and "Negociación" PR boundaries in the tasks.md Work-Unit Boundaries table, so if delivered as two separate PRs each stays comfortably under 400 on its own; a `size:exception` note is recorded here for the orchestrator/delivery-decision layer since they were implemented together in one apply batch. No commit, push, or PR was created.
- **Design deviations / action-context warnings:** the backend `ResultadoNegociacionOut` extension (join fields sourced from `precios_proveedor` via `precio_proveedor_id`, and the new `repository.py::obtener_precio_proveedor` + `service.py::_con_datos_precio_proveedor` helper) is an **additive, orchestrator-pre-approved design deviation**, not a `tasks.md` row — see the objective prompt's "Pre-approved backend fix" section. It does not change `RegistrarResultadoNegociacion`'s write contract, the `precios_proveedor` table, or the `seleccionado` feature. **Risk noted, not fixed in this objective:** `actualizar_seleccion`'s `ResultadoNegociacionOut` response is not enriched with these six fields (out of this objective's explicit scope), so a `PATCH .../seleccion` response on an already-`precio_obtenido` row will read `null` for `precio_unitario` etc. even though the row has a price; the 5.3 TRIANGULATE cache-merge task (`actualizarSeleccion` writes the returned row to the exact existing result key) should verify whether this blanks previously-fetched price data in the comparison table's cache and, if so, either enrich `actualizar_seleccion` too or have the frontend merge only the `seleccionado` field from that response instead of the whole row.
- **Remaining implementation tasks:** the first section-5.2 row is only half-complete (API/backend done, component half pending); it stays `- [ ]`. The next work is: implement `frontend/src/features/pcp/ComparacionProveedoresTable.tsx` and `RegistrarResultadoDialog.tsx` using the bounded `useQueries` fan-out now unblocked by this objective's typed client, satisfying the existing 5.1 RED test file; then the second 5.2 row (wiring `RenglonDetalle.tsx`) and section 5.3 TRIANGULATE/REFACTOR remain unchecked, followed by Consultas, Sugerencias, Legacy imports, and cross-unit validation.

## Work Unit 5 — Negociación 5.2 GREEN (both rows: component + wiring)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (22/39 complete going in). The workspace root was the sole allowed edit root. No SDD status, review, or delivery authority command was run beyond the acquired attempt token; native attempt-ledger settle is executed and confirmed after this record.
- **Completed / persisted checkboxes:** both section-5.2 rows are now visibly `[x]` in `tasks.md` (24/39 complete): the API/component row (its API/backend half was already done in the prior objective; this objective completed the component half) and the `RenglonDetalle.tsx` wiring row.
- **Files changed:** created `frontend/src/features/pcp/ComparacionProveedoresTable.tsx` and `RegistrarResultadoDialog.tsx`; modified `frontend/src/features/pcp/RenglonDetalle.tsx` (mounted the comparison table) and `RenglonDetalle.test.tsx` (added `obtenerResultado`/`actualizarSeleccion` to the existing `@/lib/api/pcp` mock with a default 404 "no result yet" resolution, plus one new wiring test); modified `ComparacionProveedoresTable.test.tsx` (see the documented test-file deviation below); `tasks.md` and this record.
- **Behavior:** `ComparacionProveedoresTable({pcpId, renglonId, proveedores, puedeEscribir})` fans out one `obtenerResultado` query per catalogued proveedor via `useQueries`, catching only `ApiError.status === 404` inside each `queryFn` and resolving to `null` ("sin resultado aún") rather than surfacing it as a query error; any other error still propagates as a normal query error. It renders a desktop `<table aria-label="Comparación de proveedores">` (one `columnheader` per proveedor code, `rowheader`s for Resultado/Precio unitario/Cantidad mín.-máx./Mantenimiento hasta/Condición-forma de pago/Motivo/Acción) and, independently, a mobile `<ul aria-label="Comparación de proveedores en tarjetas">` with one card per proveedor — both are always mounted (CSS `hidden md:table` / `md:hidden` toggles which one is visible per breakpoint; jsdom does not evaluate that CSS, so both remain queryable by role in tests). Selection toggling calls the real `actualizarSeleccion` PATCH (never local `useState`); on success the returned `seleccionado` field is merged into the *existing* cached `ResultadoNegociacion` for that proveedor via a `setQueryData` updater function (`old ? {...old, seleccionado: actualizado.seleccionado} : old`), which — per the risk flagged in the prior objective — deliberately does NOT replace the cached object with the PATCH response, so previously-fetched price/condition fields are never blanked even though `actualizar_seleccion`'s backend response omits them. `pcpQueryKeys.seleccion(pcpId)` is invalidated after every toggle so the Gestión "Negociado" badge can refresh. A 404 (`null`) column renders no toggle at all. Multiple columns can be `seleccionado=true` simultaneously (no radio/exclusive behavior anywhere). While a toggle's PATCH is in flight, that proveedor's toggle button is disabled (tracked by a single `proveedorPendiente` id, not a full-table lock). Read-only role (`puedeEscribir=false`) renders zero `RegistrarResultadoDialog` triggers and zero selection-toggle buttons in either view. Neither this component nor `RegistrarResultadoDialog` renders any close-PCP control or imports `cerrarPcp` (confirmed by `grep -n "cerrarPcp"` returning no matches across both new files and `RenglonDetalle.tsx`). `RegistrarResultadoDialog` is a self-contained Radix dialog mirroring `CrearRenglonDialog`'s shape: explicit allowlist body construction (starts from `{resultado}`, conditionally adds each optional field only when non-empty, never spreads a form object), submits via `registrarResultado` (which already normalizes FastAPI 422 arrays inside `pcp.ts`), and renders `mutation.error.message` inline via `role="alert"` without closing on failure. On success it writes the full returned row directly into the exact `pcpQueryKeys.resultado(...)` cache entry via `setQueryData` (a full replace is correct here, unlike the selection-toggle case, because `registrarResultado`'s response IS fully enriched).
- **Design deviation — accessible-name separation between desktop and mobile interactive controls (not in `tasks.md`, discovered during implementation):** the frozen 5.1 RED test asserts *exact* counts for interactive elements shared by proveedor (`getAllByText('Seleccionado')` must equal 2, not 4; `getAllByRole('button', {name: 'Quitar selección'})` must equal 2, not 4) even though both the desktop table and the mobile card list are simultaneously mounted for 2 proveedores. Rendering the literal, exactly-matching accessible name/text ("Seleccionado" tag, "Seleccionar proveedor"/"Quitar selección" button) in *both* views would double every count. Resolution: the **mobile card** carries the exact bare accessible names required by the frozen test (tag text "Seleccionado"; button names "Seleccionar proveedor" / "Quitar selección"), while the **desktop table** carries the same functionality but with a per-proveedor-*suffixed* accessible name (e.g. `Quitar selección de NORTE-15`) plus a non-text accent (`bg-emerald-50` class on the selected column, and a `title` attribute for the Resultado/Motivo cells instead of literal duplicate text) so it never collides with the frozen exact-string/regex queries. This keeps both viewports genuinely functional (unlike hiding one view's controls entirely, which would leave desktop users — the primary device for this back-office flow — without a way to act) while satisfying every frozen assertion. The `RegistrarResultadoDialog` trigger itself is left with the same bare "Registrar resultado" label in both views because no frozen test asserts an exact count for it (only *absence* for read-only, which holds regardless of duplication).
- **Test-file deviation (narrowly-scoped, documented per the objective's explicit allowance):** 3 lines in the frozen `ComparacionProveedoresTable.test.tsx` used `await screen.findByText('Precio obtenido')` purely as a "wait until the fan-out resolves" gate before their real assertions (`no muestra ningún control...`, `nunca renderiza un control para cerrar el PCP...`, `permite dos proveedores marcados...`). All three render the *default* 2-proveedor fixture with both proveedores sharing `resultado: 'precio_obtenido'`; per the "same data" design intent (mobile cards must show this literal text so `within(tarjetas[i])` scoped assertions pass), "Precio obtenido" legitimately renders twice (once per card) regardless of any desktop/mobile de-duplication — an unscoped singular `findByText` can never resolve for 2 equal-type proveedores no matter how the component is built, confirmed empirically: running the original 3 lines against the finished GREEN implementation reproduced exactly this "Found multiple elements" failure and no other, isolating it as a wait-gate defect rather than a real behavioral assertion. Each of the 3 lines was changed from `findByText` to `findAllByText` (same string, tolerant of multiple matches, identical "wait for the text to appear" semantics) with zero change to any following assertion in those tests. No other line in the frozen file was touched; the single-proveedor tests (`muestra el resultado precio_obtenido`/`no_cotiza`, still singular `findByText`) and the 404 test (singular `findByText('Sin resultado aún')`, only one proveedor has that state) were left exactly as authored and pass unmodified.
- **`RenglonDetalle.tsx` wiring:** mounted `<ComparacionProveedoresTable pcpId renglonId proveedores puedeEscribir />` directly below the existing `SeleccionProveedoresSection`, reusing the same `proveedores`/`puedeEscribir` values already computed in that component (no new local state, no `cerrarPcp` import). `RenglonDetalle.test.tsx`'s existing `@/lib/api/pcp` mock was extended with `obtenerResultado`/`actualizarSeleccion` (previously the mock only listed `obtenerDetalleRenglon`/`seleccionarProveedores`; without this addition the newly-mounted comparison table would call `undefined` and crash every existing test) and `beforeEach` now defaults `obtenerResultado` to reject with a real `ApiError('No encontrado', 404)` so the 6 pre-existing tests keep passing unchanged with the comparison table mounted (verified: no new console/act warnings). One new test proves the wiring: with proveedor `prov-1` resolving a real `precio_obtenido` result and `prov-2` still 404, a write-role user's click on the (bare-named, mobile-card) "Seleccionar proveedor" button calls `actualizarSeleccion('pcp-1', 'reng-1', 'prov-1', true)` and the "Seleccionado" tag then appears.
- **RED evidence (this objective's own TDD cycle for the new wiring assertion):** `ComparacionProveedoresTable` was temporarily unmounted from `RenglonDetalle.tsx` (the mount block was removed, then restored unchanged afterward) to capture a true RED for the new `RenglonDetalle.test.tsx` case: `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx` → **1 failed, 6 passed** — `screen.findByRole('button', {name: 'Seleccionar proveedor'})` timed out because the comparison table was not mounted, confirming this is a genuine RED against the unwired detail screen and not a broken test. Restoring the wiring turned it GREEN.
- **Verification:** safety net `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/CrearRenglonDialog.test.tsx src/features/pcp/PcpDetalle.test.tsx` → **4 files / 30 tests passed**, unchanged, before any production edit. `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx` (RED, before the 3-line wait-gate fix) → **3 failed / 7 passed** (all 3 failures were `Found multiple elements with the text: Precio obtenido` at the wait-gate line, nothing else); after the fix and with the finished component → **10/10 passed**. `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx` (RED, comparison table unmounted) → **1 failed / 6 passed**; GREEN (remounted) → **7/7 passed**. Combined required command `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/CrearRenglonDialog.test.tsx src/features/pcp/PcpDetalle.test.tsx src/features/pcp/ComparacionProveedoresTable.test.tsx` → **5 files / 41 tests passed**. `cd frontend && npm run build` → `tsc -b && vite build` passed; the nested renglón-detail route chunk grew from 12.80 kB to 16.64 kB, confirming the new components are bundled into it. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), only the same pre-existing CRLF conversion warnings on unrelated tracked files already recorded in prior entries. Static check `grep -n "cerrarPcp" frontend/src/features/pcp/RenglonDetalle.tsx frontend/src/features/pcp/ComparacionProveedoresTable.tsx frontend/src/features/pcp/RegistrarResultadoDialog.tsx` → no matches.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 5.2 row 1 — `ComparacionProveedoresTable` + `RegistrarResultadoDialog` | `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx` | Component integration | 2 files / 14 tests passed (`pcp.test.ts` + `RenglonDetalle.test.tsx`) | Whole suite unresolved (missing module) before implementation; after building the component, 3/10 failed on an ambiguous wait-gate (documented test deviation above), isolating a test-authoring defect rather than a behavioral gap | 10/10 passed after the 3-line `findAllByText` fix | The 10 frozen cases (loading, empty, 404-as-no-result with bounded 2-call assertion, precio_obtenido, no_cotiza+motivo, desktop columnheaders/rowheader, mobile cards, read-only absence, no-close-control, two-simultaneous-selected) already constitute full triangulation of every code path | None needed; this is the first GREEN pass, REFACTOR is explicitly scoped to 5.3 |
| 5.2 row 2 — wire `RenglonDetalle.tsx` | `frontend/src/features/pcp/RenglonDetalle.test.tsx` | Component integration | 6/6 pre-existing cases passed with the extended API mock, before wiring | 1 new case failed (button not found) with the comparison table temporarily unmounted | 7/7 passed after remounting `ComparacionProveedoresTable` | Single new case exercises the write-role toggle path end-to-end (`actualizarSeleccion` call + `seleccionado` tag appears); the 6 pre-existing cases already cover read-only/empty/error paths for the sibling `SeleccionProveedoresSection`, unaffected by this mount | None needed; the mount is a single additive JSX block reusing existing props |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/lib/api/pcp.test.ts src/features/pcp/RenglonDetalle.test.tsx src/features/pcp/CrearRenglonDialog.test.tsx src/features/pcp/PcpDetalle.test.tsx src/features/pcp/ComparacionProveedoresTable.test.tsx` → 5 files / 41 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` → `tsc -b && vite build` passed; `_authenticated.pcp._pcpId.renglones._renglonId` chunk grew 12.80 kB → 16.64 kB, confirming the new components are reachable from the real route tree, not just unit-tested in isolation |
| Rollback boundary | `frontend/src/features/pcp/ComparacionProveedoresTable.tsx`, `RegistrarResultadoDialog.tsx` (both new, delete to revert), the mount block + import line in `RenglonDetalle.tsx`, the mock/test additions in `RenglonDetalle.test.tsx`, the 3-line wait-gate fix in `ComparacionProveedoresTable.test.tsx`, and this record/checkbox pair only — `SeleccionProveedoresSection.tsx`, `CrearRenglonDialog.tsx`, and all other Renglones/Gestión/Catálogo files are untouched |

- **Workload / PR boundary:** self-counted (all PCP frontend files remain untracked from earlier work units, so `git diff --numstat` cannot isolate this session's lines on them; counts below are from direct `diff` against this session's pre-edit content). New files: `ComparacionProveedoresTable.tsx` 208 + `RegistrarResultadoDialog.tsx` 162 = **370**. Modified: `RenglonDetalle.tsx` +7/-0 = **7**; `RenglonDetalle.test.tsx` +32/-2 = **34**; `ComparacionProveedoresTable.test.tsx` +3/-3 = **6** (the 3 documented wait-gate line changes); `tasks.md` +2/-2 = **4** (two checkbox flips). **Total ≈ 421 changed lines**, modestly over the 400-line objective cap. Consistent with the prior 5.2 API/backend slice's precedent (~467 lines, also over budget and explicitly not trimmed), this was not compressed to fit — no comment, blank line, or test assertion was removed to hit the number; both new components are a genuinely single, cohesive unit (they satisfy one frozen 10-case test file together) and the wiring is a small, inseparable second unit. A `size:exception` note is recorded here for the orchestrator/delivery-decision layer. No commit, push, or PR was created.
- **Design deviations / action-context warnings:** two documented deviations above (desktop/mobile accessible-name separation for interactive controls; the 3-line `findAllByText` test-file fix for an ambiguous wait-gate). Neither changes any product-facing behavior asserted by the frozen spec/design; both are additive/corrective and narrowly scoped as required. No `cerrarPcp` import exists in any of the three touched/created files (confirmed by static grep). The previously-flagged selection-cache risk (PATCH response not enriched with price/condition fields) is now resolved at the frontend layer: the selection toggle's `setQueryData` updater merges only `seleccionado` into the existing cached object and never replaces it wholesale, so a previously-fetched `precio_obtenido` row's price/condition fields survive a selection toggle.
- **Native ledger:** `gentle-ai sdd-attempt settle --cwd "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" --change pcp-frontend --token sha256:e17d3395cef419932a85bea3f3ea83b6dac6ee6d61b671e572d4320465a9ff66 --outcome passed --evidence-revision sha256:8f9402b8d8565f259d439b94d8d9bd18740debc4518fcd908bad8d07bb1e21e7 ...` was run. The first attempt returned `blocked(undeclared_untracked)` listing every currently-untracked repo file (including many pre-existing ones from earlier work units) and a fresh `expected-untracked-inventory` hash (`sha256:5e621d662692bbab08c8320503cfdb6a23cb52c7c2f247cc6e861d1ac589ce74`); per the objective's explicit instruction, the retry used `--untracked-scope=select` with `--intended-untracked` naming only the two files this attempt itself created (`ComparacionProveedoresTable.tsx`, `RegistrarResultadoDialog.tsx`) and that fresh inventory hash. That retry returned `state: complete`. `gentle-ai sdd-attempt status --cwd ... --change pcp-frontend` was then run and confirmed no `active_attempt` key remains in the JSON (the top-level object now goes directly from `objective` to `attempts`), closing this work unit's ledger entry (ordinal 22, objective generation 15) cleanly.
- **Remaining implementation tasks:** section 5 (Negociación) 5.1 and 5.2 are fully `[x]` (24/39 total). The next exact unchecked rows are section 5.3: `- [ ] Add QueryClient cases proving actualizarSeleccion writes the returned row to the exact existing result key and normally invalidates only ['pcp', pcpId, 'seleccion']...` and `- [ ] Refactor comparison rendering into shared criterion data for table and mobile cards while retaining accessible selected labels and multi-select semantics...`. All later Consultas, Sugerencias, Legacy imports, and cross-unit validation rows remain unchecked in `tasks.md`.

## Work Unit 5 — Negociación 5.3 TRIANGULATE and REFACTOR (completes Work Unit 5)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the bounded section-5.3 objective (24/39 complete going in). Only `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx`, `ComparacionProveedoresTable.tsx`, `tasks.md`, and this record were touched, exactly matching the objective's named edit surface; `RenglonDetalle.tsx`, `RegistrarResultadoDialog.tsx`, `PcpDetalle.tsx`, and `queryKeys.ts` were read as-is and not modified. No SDD status, attempt, review, or delivery authority command was run beyond honoring the supplied native ledger token context; the attempt acquire/settle pair below was executed as instructed.
- **Completed / persisted checkboxes:** both section 5.3 rows are now visibly `[x]` in `tasks.md` (26/39 complete). **This completes Work Unit 5 (Negociación) entirely** — sections 5.1, 5.2, and 5.3 are all `[x]`.
- **TRIANGULATE — files changed and behavior:** added two QueryClient-spy cases to `ComparacionProveedoresTable.test.tsx` (pattern mirrors `RenglonDetalle.test.tsx`'s "Exact renglón invalidation" case — real `QueryClient`, `vi.spyOn` on `setQueryData`/`invalidateQueries`, no mocked query client). Case 1 marks a provider as `seleccionado` and proves: `setQueryData` is called with the exact existing `pcpQueryKeys.resultado(pcpId, renglonId, proveedorId)` key, the resulting cached row keeps its previously-fetched `precio_unitario`/`cantidad_minima` (not blanked), and `invalidateQueries` is called with `{ queryKey: pcpQueryKeys.seleccion(pcpId) }` and NOT with the detalle, renglones, renglon, or resultado keys. Case 2 repeats the same assertions for the symmetric unmark (`seleccionado: true → false`) path and additionally asserts `invalidateQueries` was called exactly once, confirming no broader tree is ever touched either way. No production code change was required for these two cases — the existing `alternarSeleccion` implementation (from the prior 5.2 objective) already satisfied the exact contract; the tests are close to approval tests for genuinely working code, not exploratory RED-only assertions.
- **Non-trivial-assertion sanity check (not part of the persisted TDD cycle, run and reverted to validate the new tests are not vacuous):** temporarily changed `ComparacionProveedoresTable.tsx`'s `invalidateQueries` call from `pcpQueryKeys.seleccion(pcpId)` to `pcpQueryKeys.detalle(pcpId)` (i.e. `['pcp', pcpId]` instead of `['pcp', pcpId, 'seleccion']`) and re-ran the focused test. Both new cases genuinely failed: because TanStack Query's default (non-`exact`) `invalidateQueries` prefix-matches every query key starting with the given array, invalidating `['pcp', pcpId]` also matched the currently-mounted `resultado` query itself (whose key is `['pcp', pcpId, 'renglones', renglonId, 'resultado', proveedorId]`, which starts with `['pcp', pcpId]`), triggering an automatic refetch that raced with and overwrote the manual `setQueryData` merge — `getQueryData` then read back the pre-toggle `seleccionado` value instead of the toggled one. This is real, non-tautological evidence that "invalidate only the narrow `seleccion` key, never a broader ancestor" is a behaviorally load-bearing contract, not a stylistic preference: a broader invalidate would silently reintroduce exactly the stale-price-on-selection-toggle bug flagged as a risk in the 5.2 API/backend objective's record. The mutation was reverted immediately after confirming the failure; no other file was touched during this check.
- **REFACTOR — shared criterion data:** extracted a `CRITERIOS_COMPARACION: CriterioComparacion[]` array in `ComparacionProveedoresTable.tsx` (`{ clave, etiqueta, valor(resultado) }`) covering the four criteria that were previously hardcoded twice with identical labels and near-identical value logic (Precio unitario, Cantidad mín./máx., Mantenimiento hasta, Condición/forma de pago). The desktop `<table>` now renders `{CRITERIOS_COMPARACION.map((criterio) => <tr>...)}` instead of four separate hardcoded `<tr>` blocks; the mobile `<dl>` now renders `{CRITERIOS_COMPARACION.map((criterio) => <div>...)}` instead of four separate hardcoded `<div>` pairs. Each `valor()` function reproduces the exact prior string/value (e.g. `` `${cantidad_minima ?? '—'} / ${cantidad_maxima ?? '—'}` ``), so every rendered label and value is byte-identical to before; only the single source of truth changed, not the output. The "Resultado" row/text (icon + title, already partly shared via the pre-existing `etiquetaResultado()` helper), the "Motivo" row (title-attribute vs. plain-text asymmetry between views), the "Acción" row/controls, and the accessible selected-state labels (`Seleccionado` tag, `Quitar selección [de X]` / `Seleccionar proveedor [X]` buttons) were deliberately left untouched — they are not uniform-shaped across both views (icon vs. text, conditional per-view suffixing for accessible-name collision avoidance from the prior 5.2 objective) and folding them into the same structure would either change rendered output or reduce readability without reducing real duplication. This satisfies "a future criterion change only happens in one place" for the genuinely duplicated criteria without forcing an artificial one-size-fits-all abstraction onto the asymmetric rows.
- **Verification:** safety net `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx src/features/pcp/PcpDetalle.test.tsx src/features/pcp/RenglonDetalle.test.tsx` (pre-edit) → **3 files / 25 tests passed**. TRIANGULATE GREEN (new cases against the unmodified 5.2 implementation): `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx` → **1 file / 12 tests passed** immediately (10 prior + 2 new). Sanity-check RED (temporary `detalle` key mutation): same command → **2 failed / 10 passed**, both failures showing the stale pre-toggle `seleccionado` value from the reintroduced refetch race, confirmed non-trivial. GREEN after revert: same command → **1 file / 12 tests passed**. REFACTOR verification: `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx src/features/pcp/PcpDetalle.test.tsx` → **2 files / 20 tests passed**, zero behavioral change beyond the 2 new TRIANGULATE cases. `cd frontend && npm run build` → `tsc -b && vite build` passed. Required combined command `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx src/features/pcp/PcpDetalle.test.tsx src/features/pcp/RenglonDetalle.test.tsx` → **3 files / 27 tests passed**. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), only the same pre-existing CRLF conversion warnings on unrelated tracked files already recorded in prior entries.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 5.3 TRIANGULATE — selection cache contract | `frontend/src/features/pcp/ComparacionProveedoresTable.test.tsx` | Component integration (real `QueryClient` spy) | 3 files / 25 tests passed | N/A — approval-style cases against already-correct 5.2 production code; genuine RED demonstrated separately via a temporary, reverted production mutation (documented above) rather than via a missing-code gap | 12/12 passed on first run against unmodified code | Mark (`false→true`) and unmark (`true→false`) are the two distinct toggle directions; both assert the same exact-key write, price-field preservation, and single narrow invalidation | N/A for this row — REFACTOR is the separate second 5.3 row below |
| 5.3 REFACTOR — shared criterion data | `frontend/src/features/pcp/ComparacionProveedoresTable.tsx` | Component (desktop table + mobile cards) | 12/12 (post-TRIANGULATE) passed before refactor | N/A — pure refactor, no new test | 20/20 passed (`ComparacionProveedoresTable.test.tsx` + `PcpDetalle.test.tsx`), zero assertion changes needed | N/A — behavior-preserving by construction (identical `valor()` output per criterion) | `CRITERIOS_COMPARACION` array now the single source for the four duplicated criteria; desktop/mobile each map over it instead of repeating four hardcoded blocks |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx src/features/pcp/PcpDetalle.test.tsx src/features/pcp/RenglonDetalle.test.tsx` → 3 files / 27 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` → `tsc -b && vite build` passed, confirming the refactored component still type-checks and bundles into its existing route chunk |
| Rollback boundary | The two new TRIANGULATE cases and the `CRITERIOS_COMPARACION` refactor in `ComparacionProveedoresTable.test.tsx`/`ComparacionProveedoresTable.tsx`, plus the two section-5.3 checkboxes in `tasks.md` and this record — no other Negociación, Renglones, or Gestión file was touched |

- **Workload / PR boundary:** bounded 5.3-only slice, completing Work Unit 5; authored delta is small: `ComparacionProveedoresTable.test.tsx` +49/-0 = **49**; `ComparacionProveedoresTable.tsx` refactor +33/-31 = **64** (net +2 lines; four duplicated blocks replaced by one shared array plus two `.map()` call sites); `tasks.md` +2/-2 = **4**. **Total ≈ 117 changed lines**, comfortably below the 400-line budget. Rollback boundary is exactly the two source files plus the checkbox/record updates above. No commit, push, PR, or later work unit (Consultas, section 6+) was started.
- **Design deviations / action-context warnings:** none. The sanity-check mutation-and-revert (documented above) touched production code transiently during verification only; the file was restored to its exact pre-check state and the final diff contains no trace of it — confirmed by the final `git diff --check` pass and by the REFACTOR verification re-running clean immediately after.
- **Remaining implementation tasks:** Work Unit 5 (Negociación) is now fully `[x]` (26/39 total). The next exact unchecked row is section 6.1: `- [ ] Create failing \`frontend/src/features/pcp/ConsultaDetalle.test.tsx\` and extend \`frontend/src/lib/api/pcp.test.ts\` for grouping selected renglones into one consultation per provider, read-only hidden grouping/send actions, binary PDF download, and send success/failure feedback without messaging-adapter detail; verify with \`cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/lib/api/pcp.test.ts\`; rollback the consultation test additions. <!-- sdd-owner: implementation -->\`. All later Consultas, Sugerencias, Legacy imports, and cross-unit validation rows remain unchecked in `tasks.md`.

## Corrective Rerun — Native review R3-fanout-error-masking (Comparación de proveedores)

- **Status / safety:** first native `gentle-ai review` (receipt-driven development) lineage run to completion in this session, using a fixed dev-binary (PR #3998, commit `d9189c30f0cdbb0e6ee850cd07a533d79dec14f5`) invoked by full path to bypass the known stable-`v2.7.0` untracked-handoff loop. Lineage `review-05e7a8875e541c5e`, medium risk, 1 lens (`review-reliability`), correction budget 200 lines. User explicitly granted the `gentle-ai.review-integration.consent/v3` envelope before START.
- **Finding corrected (CRITICAL, candidate-caused, corroborated):** `R3-fanout-error-masking` — in `ComparacionProveedoresTable.tsx`, the per-proveedor `useQueries` fan-out only gated on `isPending`; a non-404 fetch failure (`isError: true`, `data: undefined`) rendered identically to "no result yet" (`Sin resultado aún`, no toggle), silently misinforming compras/gerencia users comparing suppliers.
- **Fix:** added an `error: boolean` field to `Columna` (from `consultas[index].isError`); desktop table cell now shows `⚠` / "Error al cargar el resultado" (red text, distinct from `—` / "Sin resultado aún"); mobile card shows a `role="alert"` red message in the same case. No change to `obtenerResultadoOSinDato`'s 404-as-null behavior.
- **Focused coverage:** new `ComparacionProveedoresTable.test.tsx` case simulates a 500 (non-404) rejection for one proveedor and asserts "Error al cargar el resultado" renders while "Sin resultado aún" does not, alongside the other proveedor's normal result.
- **Verification:** RED failed exactly on the missing distinct error text (**1 failure, 12 passes**). GREEN: `cd frontend && npm run test -- src/features/pcp/ComparacionProveedoresTable.test.tsx src/features/pcp/PcpDetalle.test.tsx src/features/pcp/RenglonDetalle.test.tsx` → **3 files / 28 tests passed**. `cd frontend && npm run build` → passed (one intermediate `tsc` error on a partial-`Columna` object literal at the mobile toggle call site was fixed by adding `error: false`). `git diff --check` → clean.
- **Native review outcome:** targeted validator ran, state moved `correction_required` → `approved` → `acknowledged` (authority burned, receipt `sha256:ff1e22165cc3f0401f09904dfa77e927840230e946efd839f7b0794bd021bdc7`). Two non-blocking advisory findings surfaced for later, separate work (not corrected here, not reopening this review): `R3-state-transition-cache-staleness` (SUGGESTION, `EstadoPcpStepper.tsx:17-19`) and `R3-toggle-error-handling` (WARNING, `ComparacionProveedoresTable.tsx:95-109` — `alternarSeleccion`'s try/finally does not surface a failed `actualizarSeleccion` PATCH to the user).

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| R3-fanout-error-masking correction | `ComparacionProveedoresTable.test.tsx` | Component integration | 12/12 passed before the new case | Distinct error text absent; identical to no-result | 28/28 passed (3 files) | Non-404-vs-404-vs-success are three distinct fan-out outcomes now exercised | None needed — the fix is additive (`error` field), no existing branch restructured |

- **Workload / PR boundary:** correction-only slice, bounded by the native review's own 200-line budget (declared 60, actual delta well under that): `ComparacionProveedoresTable.tsx` +~15/-6; `ComparacionProveedoresTable.test.tsx` +11/-0. Rollback boundary is exactly these two files. No task checkbox changed — this is a quality correction on already-`[x]` Work Unit 5, not new task-list scope. No commit, push, or PR was created.
- **Design deviations / action-context warnings:** none — the fix is scoped exactly to the finding's claim, no other rendering path changed.
- **Remaining implementation tasks:** unchanged — next is section 6.1 (Consultas), as above. The two advisory findings above are noted as candidate follow-up work, not queued as task-list rows.

## Corrective Rerun — Both non-blocking advisory findings from the native review

- **Status / safety:** addressed at user request ("vamos primero con los hallazgos") before starting Work Unit 6. Not part of the acknowledged review lineage (already closed/burned) — a plain TDD quality correction, same as prior Corrective Rerun entries. No SDD status, authority, or review command was run for this slice.
- **R3-toggle-error-handling (WARNING, `ComparacionProveedoresTable.tsx:95-109`):** `alternarSeleccion`'s `try/finally` had no `catch`, so a rejected `actualizarSeleccion` PATCH (network failure, 403, etc.) surfaced nothing to the user — the button just stopped being disabled with zero feedback (confirmed via an actual unhandled-rejection in the RED run). Fixed by adding `proveedorConError` state, a `catch` branch, and an inline `role="alert"` message ("No se pudo actualizar la selección.") next to the toggle in both desktop and mobile views.
- **R3-state-transition-cache-staleness (SUGGESTION, `EstadoPcpStepper.tsx:17-19`):** `cambiarEstadoPcp`'s `onSuccess` only wrote `pcpQueryKeys.detalle(pcp.id)`, unlike `CerrarPcpDialog`'s established convention of also merging into every `pcpQueryKeys.listas()` entry — so advancing a PCP's estado left the Gestión list view showing the stale prior estado until an unrelated refetch. Fixed by mirroring `CerrarPcpDialog`'s exact `setQueriesData<Pcp[]>` merge pattern.
- **Focused coverage:** new `PcpDetalle.test.tsx` case seeds two list-cache entries, advances state via the stepper, and asserts both list entries reflect the new estado. New `ComparacionProveedoresTable.test.tsx` case forces `actualizarSeleccion` to reject and asserts the inline error message renders (using `findAllByText`, since both the always-mounted desktop and mobile views render the same text for a single-proveedor fixture — same pattern already established in the 5.2 slice).
- **Verification:** both RED failures confirmed exactly the diagnosed gaps (list cache still read `'nueva'`; the rejection went fully unhandled). GREEN: `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **8 files / 61 tests passed**. `cd frontend && npm run build` → passed. `git diff --check` → clean.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| R3-toggle-error-handling | `ComparacionProveedoresTable.test.tsx` | Component integration | 4 files/32 passed before this case | Unhandled rejection, no user-visible feedback | 61/61 (full PCP suite) passed | N/A — single bounded fix | None needed |
| R3-state-transition-cache-staleness | `PcpDetalle.test.tsx` | Component + QueryClient | Same safety net | List cache entries stayed `'nueva'` | 61/61 (full PCP suite) passed | N/A — single bounded fix | None needed — mirrors `CerrarPcpDialog`'s existing pattern exactly |

- **Workload / PR boundary:** both findings fixed in one small bounded slice (~35 changed lines across `EstadoPcpStepper.tsx`, `ComparacionProveedoresTable.tsx`, and their two test files). Rollback boundary is exactly these four files. No task checkbox changed — quality corrections only. No commit, push, or PR was created.
- **Design deviations:** none — both fixes reuse patterns already established elsewhere in this same feature (`CerrarPcpDialog`'s list-merge; the dialogs' `role="alert"` inline-error convention).
- **Remaining implementation tasks:** unchanged — next is section 6.1 (Consultas). No advisory findings remain open.

## Work Unit 6 — Consultas 6.2 GREEN (both rows: API/components + renglón mount)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the bounded section-6.2 objective (26/39 complete going in, since 6.1 RED was already `[x]`). The workspace root was the sole allowed edit root. No SDD status, attempt, review, or delivery authority command was run beyond honoring the supplied native ledger token context.
- **Completed / persisted checkbox:** the single section-6.2 row is now visibly `[x]` in `tasks.md` (27/39 complete). This row covers both the API/component implementation and the explicit "expose the grouping entry only from eligible PCP/renglón flow" mounting requirement.
- **Files changed:** `frontend/src/lib/api/pcp.ts` (added `Consulta`, `SeleccionParaAgrupar`, `AgruparConsultaPayload` types and `agruparConsultas`/`obtenerConsulta`/`enviarConsulta`/`descargarPdfConsulta`); created `frontend/src/features/pcp/AgruparConsultaDialog.tsx`, `ConsultaDetalle.tsx`, and `frontend/src/routes/_authenticated.pcp.consultas.$consultaId.tsx`; modified `frontend/src/features/pcp/ComparacionProveedoresTable.tsx` (exported the existing private `obtenerResultadoOSinDato` helper, no behavior change), `RenglonDetalle.tsx` (mounted the grouping entry), `RenglonDetalle.test.tsx` (three new cases), `frontend/src/lib/api/pcp.test.ts` (one-line test-isolation defect fix, documented below); regenerated `frontend/src/routeTree.gen.ts`; `tasks.md` and this record.
- **Behavior — `lib/api/pcp.ts`:** `agruparConsultas` builds its body starting from `{selecciones}` and conditionally adds `contacto_id`/`fecha_respuesta_esperada`/`canal` only when defined (same explicit-allowlist convention as `crearRenglon`/`registrarResultado`), POSTs to `/pcp/consultas`. `obtenerConsulta`/`enviarConsulta` are plain typed `presupuestacionFetch` calls to their exact routes. `descargarPdfConsulta` cannot reuse `presupuestacionFetch` (it always calls `.json()`, per design.md); it duplicates the auth-header fetch pattern directly against `@/lib/supabase`'s `getSession()` and returns `response.blob()`, never parsing JSON, and throws an `ApiError` on a non-ok response without ever calling `presupuestacionFetch`.
- **Behavior — `ConsultaDetalle.tsx`:** reads `obtenerConsulta` via `pcpQueryKeys.consulta(consultaId)`; renders proveedor/estado; "Descargar PDF" is visible to every read role (no role gate, per spec) and triggers a blob download via a temporary anchor element, guarded against environments without `URL.createObjectURL` (jsdom does not implement it) so the download side-effect never crashes the render; "Enviar consulta" is rendered only when `puedeRol(perfil?.rol, PCP_WRITE_ROLES)`, shows a plain "Consulta enviada." confirmation on success (no messaging-adapter/channel text) and an inline `role="alert"` message on failure while the button stays enabled for retry. On send success, the returned `Consulta` is merged into the exact `pcpQueryKeys.consulta(consultaId)` cache entry via `setQueryData`.
- **Behavior — `AgruparConsultaDialog.tsx`:** takes an explicit `puedeEscribir` prop (documented design deviation from the 6.1 RED entry, reaffirmed here) instead of being conditionally mounted by its parent, because it is now genuinely reused as a renglón-scoped action and centralizes the role check once. Renders no trigger at all when `!puedeEscribir`. On confirm, calls `agruparConsultas({selecciones})` with the exact `selecciones` prop, no extra keys. On success, seeds `pcpQueryKeys.consulta(consulta.id)` for every returned consultation (targeted cache updates, never a broader invalidate) and renders a list of "Ver consulta de {proveedor_id}" links to `/pcp/consultas/{id}`, since D6 established there is no consultas index route — this is the only way to reach a newly created consultation's detail screen.
- **Design decision — grouping-entry mount site (the "expose only from eligible PCP/renglón flow" requirement):** mounted in `RenglonDetalle.tsx` only, scoped to the current renglón. `RenglonDetalle` already fetches `proveedores_catalogados`; it now also runs a `useQueries` fan-out over `obtenerResultadoOSinDato` using the *exact same* `pcpQueryKeys.resultado(pcpId, renglonId, proveedorId)` keys that `ComparacionProveedoresTable` already subscribes to — React Query dedupes by key, so this adds zero extra network calls, only an additional subscriber to the same cache entries. `seleccionesConsulta` is derived as `{pcp_renglon_id: renglonId, proveedor_id}` for every catalogued proveedor whose cached result has `seleccionado === true` (the same "Negociado" concept that already powers the Gestión badge, per the launch prompt's explicit pointer to "the Negociado badge/selection summary work from Work Unit 4/5"). The trigger is rendered only when `seleccionesConsulta.length > 0`, so a renglón with no negotiated selection never shows a pointless empty-grouping action; `puedeEscribir` is still passed through and independently re-checked inside `AgruparConsultaDialog`. **Scope explicitly not implemented in this row:** a cross-renglón/cross-PCP multi-select grouping UI at `PcpDetalle.tsx` (the literal "three renglones on one PCP" wording in the spec's first scenario) was not built, because (a) no frozen RED test exercises that granularity — `ConsultaDetalle.test.tsx`'s `AgruparConsultaDialog` tests only exercise the dialog itself given an already-computed `selecciones` prop; (b) `PcpDetalle`'s existing queries (`listarRenglones`, `listarRenglonesSeleccionados`) return renglón ids only, never proveedor ids, so building that UI would require a new nested fan-out (renglón × proveedor) or a new backend endpoint, either of which is a materially larger, differently-scoped unit than this bounded row's budget; (c) the single-renglón implementation still satisfies the spec's literal requirement text ("group... into a consultation in a single `POST /pcp/consultas` call... gated by write role") for the smallest valid case (one or more renglón+proveedor pairs from one renglón), and the backend already supports grouping selections that happen to span multiple renglones/PCPs in one call if a future PCP-level UI supplies them — nothing in this row's implementation blocks that later extension. This is flagged as a candidate follow-up, not a silent gap.
- **Test-authoring defect found and fixed (frozen `pcp.test.ts`):** the `descargarPdfConsulta (descarga binaria)` describe block's `beforeEach` reset `fetchMock` and `getSessionMock` but never `presupuestacionFetch`, whose mock call history is shared across the whole file (single `vi.fn()` instance from the top-level `vi.mock('./presupuestacion', ...)` factory). Because the preceding `cliente PCP` describe block runs first in the same file and its last test calls `enviarConsulta` (which does use `presupuestacionFetch`), that call leaked into the first `descargarPdfConsulta` test's mock history, making `expect(presupuestacionFetch).not.toHaveBeenCalled()` fail under any correct implementation (confirmed empirically: ran the finished, correct `descargarPdfConsulta` against the original unfixed test and got exactly this failure, isolating it as a test-isolation defect rather than a behavioral gap). Fixed with a single added line — `vi.mocked(presupuestacionFetch).mockReset()` — inside that describe's existing `beforeEach`, mirroring the sibling `fetchMock`/`getSessionMock` reset pattern already present. No assertion, expected call, or test intent was changed.
- **Verification:** safety net (before any edit) `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **7 files / 61 tests passed unchanged, 2 files / 5 tests RED** — exactly matched the prior session's reported starting point. `cd frontend && npm run test -- src/lib/api/pcp.test.ts` after the four new client functions → **1 failure remaining** (the test-isolation defect above); after the one-line fix → **19/19 passed**. `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/lib/api/pcp.test.ts` → **2 files / 27 tests passed**, fully GREEN, no frozen assertion weakened. RED for the new `RenglonDetalle.test.tsx` grouping-mount cases (before touching `RenglonDetalle.tsx`) → **1 failed (the positive mount case, missing trigger), 9 passed** (the two negative "hidden" cases pass trivially without production code, matching the established pattern for negative RED cases in this change). GREEN after mounting → `cd frontend && npm run test -- src/features/pcp/RenglonDetalle.test.tsx` → **10/10 passed**. Full required command `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **9 files / 77 tests passed**. `npx vite build` regenerated `routeTree.gen.ts` (confirmed the new `_authenticated.pcp.consultas.$consultaId` route chunk), then `cd frontend && npm run build` (`tsc -b && vite build`) → passed, same chunk present as `_authenticated.pcp.consultas._consultaId-*.js`. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), only the same pre-existing CRLF conversion warnings on unrelated tracked files already recorded in prior entries.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 6.2 — typed consultation client (`agruparConsultas`/`obtenerConsulta`/`enviarConsulta`/`descargarPdfConsulta`) | `frontend/src/lib/api/pcp.test.ts` | API contract | 7 unaffected files / 61 tests passed | 5 pre-existing RED cases (from 6.1) failed on missing client methods | 19/19 passed after implementation + the one-line test-isolation fix | The 5 frozen cases already cover multi-provider grouping, id-exact GET, POST-exact route, successful binary download with auth header, and non-ok binary rejection | None needed; `descargarPdfConsulta` intentionally duplicates the auth-header fetch pattern per design.md, not shared with `presupuestacionFetch` |
| 6.2 — `ConsultaDetalle` + `AgruparConsultaDialog` | `frontend/src/features/pcp/ConsultaDetalle.test.tsx` | Component integration | N/A (new file, no pre-existing baseline in this file) | Frozen suite failed to resolve `./ConsultaDetalle`/`./AgruparConsultaDialog` (from 6.1) | 27/27 passed (2 files) on first implementation pass, no frozen assertion changed | The 8 frozen cases already cover grouping confirmation, read-only trigger absence, detail render, PDF download without raw-blob exposure, write-role send visibility, read-role send absence, send success confirmation, and send failure-keeps-usable | None needed; first GREEN pass, REFACTOR explicitly deferred to 6.3 |
| 6.2 — grouping-entry mount in `RenglonDetalle.tsx` (new behavior, not covered by frozen tests) | `frontend/src/features/pcp/RenglonDetalle.test.tsx` | Component integration | 9/9 pre-existing cases passed before the 3 new cases | 1 of 3 new cases failed (missing "Agrupar consulta" trigger); the 2 negative cases passed trivially (no button anywhere yet) | 10/10 passed after mounting `AgruparConsultaDialog` with the derived `seleccionesConsulta` | Write-role-with-selection (positive), write-role-without-selection (hidden), and read-role-with-selection (hidden) are three distinct gating paths | None needed; the fan-out reuses the exact existing `pcpQueryKeys.resultado` keys and `obtenerResultadoOSinDato` helper, zero duplicated network logic |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → 9 files / 77 tests passed |
| Runtime harness command/scenario and exact result | `npx vite build` (routeTree regeneration) then `cd frontend && npm run build` (`tsc -b && vite build`) → passed; confirmed the new `_authenticated.pcp.consultas.$consultaId` route present in `routeTree.gen.ts` and bundled into its own chunk |
| Rollback boundary | `frontend/src/lib/api/pcp.ts` (the four new functions + three new types, additive only), `frontend/src/features/pcp/AgruparConsultaDialog.tsx` (new, delete to revert), `ConsultaDetalle.tsx` (new, delete to revert), `frontend/src/routes/_authenticated.pcp.consultas.$consultaId.tsx` (new, delete to revert), the one-line `export` addition in `ComparacionProveedoresTable.tsx`, the fan-out + mount block in `RenglonDetalle.tsx`, the three new cases in `RenglonDetalle.test.tsx`, the one-line defect fix in `pcp.test.ts`, and the regenerated `routeTree.gen.ts` entry — no other Gestión/Catálogo/Renglones/Negociación file was touched |

- **Workload / PR boundary:** self-counted (all touched PCP files remain untracked or already-modified from earlier work units, so `git diff --numstat` cannot cleanly isolate this session's lines): `pcp.ts` +76; `AgruparConsultaDialog.tsx` +71 (new); `ConsultaDetalle.tsx` +85 (new); the nested route +9 (new); `ComparacionProveedoresTable.tsx` +4/-1 (export + comment); `RenglonDetalle.tsx` +21/-7; `RenglonDetalle.test.tsx` +45/-2 (three new cases + mock wiring); `pcp.test.ts` +5/-0 (defect-fix comment + one line); `tasks.md` +1/-1. **Total ≈ 317 changed lines**, below the 400-line budget; the generated `routeTree.gen.ts` diff is excluded from authored risk count per the review-budget guard. Rollback boundary is exactly the files listed above. No commit, push, PR, or later work unit (6.3, Sugerencias, Imports) was started.
- **Design deviations / action-context warnings:** two documented above — (1) `AgruparConsultaDialog`'s explicit `puedeEscribir` prop instead of parent-conditional mounting (reaffirms the 6.1 RED entry's decision, now exercised by a real second consumer); (2) the grouping entry is mounted only from the single-renglón `RenglonDetalle` flow, not from a `PcpDetalle`-level cross-renglón selector, explicitly flagged as a scoped, spec-compliant-at-minimum-granularity follow-up candidate rather than a silent gap. The one-line `pcp.test.ts` test-isolation fix is a documented, narrowly-scoped correction of a genuine defect (missing mock reset), not a weakening of any frozen assertion.
- **Remaining implementation tasks:** the next exact unchecked row is section 6.3's first item: `- [ ] Add consultation tests for several providers yielding several returned consultations, a PDF fetch failure, and send failure recovery that keeps the action usable; verify focused Vitest and \`cd frontend && npm run build\`; rollback the added edge-case tests. <!-- sdd-owner: implementation -->` followed by its REFACTOR row. Sugerencias, Legacy imports, and cross-unit validation remain unchecked in `tasks.md`.

## Corrective Rerun — Consultas grouping scope (PCP-level, not per-renglón)

- **Status / safety:** consumed the orchestrator-injected, parent-authoritative OpenSpec `pcp-frontend` apply-ready context for the already-checked 6.2 row's implementation (27/39 tasks visibly `[x]` going in, unchanged by this correction — the row's literal text does not mandate a specific mount site). The repository root was the sole allowed edit root. Native ledger: `gentle-ai sdd-attempt acquire --token sha256:34ea0bd442cd9e4c22665480f6fdfad65fe31b996db4ef02a874a5e318ebf823` returned `state: proceed`, continuing active attempt ordinal 26 (`work_unit: consultas-6.2-scope-correction`); `gentle-ai sdd-attempt settle --token <same> --outcome passed` returned `state: complete`; a subsequent `gentle-ai sdd-attempt status` confirmed no `active_attempt` block remained.
- **Gap being corrected:** the prior 6.2 GREEN slice mounted `AgruparConsultaDialog` from `RenglonDetalle.tsx`, deriving `selecciones` from only one renglón's own catalogued proveedores. This did not satisfy the spec's own minimum scenario (`specs/pcp-ui-consultas/spec.md`, "Group renglones from a single PCP"): *"GIVEN three renglones on one PCP assigned to the same proveedor... THEN the system creates one consultation containing all three renglones."* Grouping must span all renglones in the PCP, not one renglón at a time.
- **Completed / persisted checkbox:** no `tasks.md` row changed — this corrects the already-checked 6.2 row's implementation; it is not new task-list scope. `grep -c "^- \[x\]" openspec/changes/pcp-frontend/tasks.md` → **27** (unchanged, confirmed both before and after this correction).
- **Files changed (additive backend, moved-mount frontend):**
  - Backend (additive only, `GET /pcp/{pcp_id}/seleccion` and its `list[str]` contract for the Gestión badge are completely untouched): `services/pcp/negociacion/repository.py` (new `listar_pcp_renglon_proveedor_seleccionados`), `services/pcp/negociacion/service.py` (new `listar_selecciones_agrupables` + `listar_selecciones_agrupables_para_endpoint`), `services/pcp/negociacion/models.py` (new `SeleccionAgrupable`), `services/pcp/negociacion/router.py` (new `GET /pcp/{pcp_id}/selecciones-agrupables`), `tests/pcp/negociacion/test_service.py` (+2 tests), `tests/pcp/negociacion/test_router.py` (+1 test).
  - Frontend client: `frontend/src/lib/api/pcp.ts` (new `SeleccionAgrupable` type + `listarSeleccionesAgrupables`), `frontend/src/lib/api/pcp.test.ts` (+1 test), `frontend/src/features/pcp/queryKeys.ts` (new `seleccionesAgrupables` key).
  - Mount moved: `frontend/src/features/pcp/RenglonDetalle.tsx` (removed the `AgruparConsultaDialog` mount and its dedicated `useQueries` fan-out over `obtenerResultadoOSinDato`), `frontend/src/features/pcp/RenglonDetalle.test.tsx` (removed the 3 now-wrong-scope grouping-mount cases and the `agruparConsultas` mock, kept everything else), `frontend/src/features/pcp/ComparacionProveedoresTable.tsx` (reverted `obtenerResultadoOSinDato` back to non-exported — confirmed nothing else imports it after the `RenglonDetalle` revert), `frontend/src/features/pcp/PcpDetalle.tsx` (new `listarSeleccionesAgrupables` query + `AgruparConsultaDialog` mount next to "Renglones"/`CrearRenglonDialog`), `frontend/src/features/pcp/PcpDetalle.test.tsx` (+3 tests). `ConsultaDetalle.tsx`, `ConsultaDetalle.test.tsx`, `AgruparConsultaDialog.tsx` itself, the nested consultation route, and all already-passing consultation API client methods were not touched.
- **Behavior:** `GET /pcp/{pcp_id}/selecciones-agrupables` (`require_roles(*ROLES_LECTURA_PCP)`, mirrors `listar_renglones_seleccionados_endpoint` exactly) returns every `{pcp_renglon_id, proveedor_id}` pair across the whole PCP where `seleccionado=true`, without collapsing by renglón (a renglón can have more than one selected proveedor, Work Unit 5) — distinct from the frozen `GET /pcp/{pcp_id}/seleccion` (`list[str]`, deduplicated by renglón, feeds only the "Negociado" badge). `PcpDetalle` reads this list with its own `useQuery`/query key and mounts `AgruparConsultaDialog` with `selecciones={data ?? []}` and `puedeEscribir={puedeRol(perfil?.rol, PCP_WRITE_ROLES)}` only when write-role is true AND the list is non-empty — gating logic moved verbatim from the removed `RenglonDetalle` mount, now evaluated at the correct (PCP-wide) scope. `AgruparConsultaDialog` itself, `agruparConsultas`, and the backend's per-proveedor `Consulta[]` grouping in `consultas/service.py::agrupar_renglones` are unchanged; the frontend's only job was gathering the full-PCP pair list in one query instead of one renglón's own catalogued proveedores.
- **Verification:**
  - Backend safety net (before any change): `./venv/Scripts/python.exe -m pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` → **22 passed**, unchanged from the last recorded baseline.
  - Backend RED (2 new service tests + 1 new router test added before any production change): same command → collection error, `ImportError: cannot import name 'listar_selecciones_agrupables' from 'services.pcp.negociacion.service'` — the expected RED (missing production symbol, not a test-logic failure).
  - Backend GREEN: same command → **25 passed**, 22 warnings (same pre-existing `supabase` deprecation warnings as baseline); the 3 new cases prove a renglón with two selected proveedores yields two pairs, an unselected renglón yields none, and a read-role (`gerencia`) can call the new endpoint successfully while the existing `/seleccion` endpoint's own tests (deduplication, 403 for read role, 404 for unknown pair) all still pass unchanged.
  - Frontend safety net (before any change): `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **9 files / 77 tests passed**, matching the last recorded baseline exactly.
  - Frontend RED (targeted, honest evidence): after writing all frontend test additions and implementing `pcp.ts`/`PcpDetalle.tsx`/`RenglonDetalle.tsx` together, the mount site's positive case was independently re-verified RED by temporarily reverting only the `AgruparConsultaDialog` mount block in `PcpDetalle.tsx` and running `cd frontend && npm run test -- src/features/pcp/PcpDetalle.test.tsx` → **1 failed, 11 passed** (the positive "shows the grouping trigger" case failed on the missing button; the two negative "hides..." cases passed trivially, matching this change's established RED pattern for negative cases). The mount block was then restored.
  - Frontend GREEN: `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **9 files / 78 tests passed** (77 baseline − 3 removed `RenglonDetalle.test.tsx` cases + 1 new `pcp.test.ts` case + 3 new `PcpDetalle.test.tsx` cases = 78, arithmetic confirmed).
  - `cd frontend && npm run build` (`tsc -b && vite build`) → passed.
  - `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), reporting only the same pre-existing CRLF conversion warnings on unrelated tracked files already logged in prior entries.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| Backend `listar_selecciones_agrupables` + endpoint | `tests/pcp/negociacion/test_service.py`, `tests/pcp/negociacion/test_router.py` | Service + API contract (pytest, integration) | 22/22 passed | Collection `ImportError` on the missing service symbol | 25/25 passed | Two-selected-proveedores-on-one-renglón case and zero-selection case are distinct code paths; router case adds the read-role access path | None needed; mirrors `listar_renglones_seleccionados`'s existing shape exactly |
| `listarSeleccionesAgrupables` client | `frontend/src/lib/api/pcp.test.ts` | API contract | part of the 77/77 baseline | N/A — new function, exact-route assertion written directly against the not-yet-existing export | 78/78 passed (whole required command) | Single scenario (two pairs, one proveedor) is sufficient: pure passthrough GET, no branching to triangulate | None needed |
| Mount moved to `PcpDetalle` | `frontend/src/features/pcp/PcpDetalle.test.tsx` | Component integration | part of the 77/77 baseline; isolated re-run 11/11 passed with the mount block absent | Independently reverted the mount block post hoc and confirmed 1/12 failure (positive case) with the two negative cases passing trivially, matching this change's documented pattern | 78/78 passed (whole required command) after restoring the mount | Positive (write-role, non-empty, two renglones on one proveedor merged into one `agruparConsultas` call), negative-empty, and negative-read-role are three distinct gating paths | None needed; gating logic moved verbatim from the removed `RenglonDetalle` mount |
| Mount removed from `RenglonDetalle` | `frontend/src/features/pcp/RenglonDetalle.test.tsx` | Component integration | 10/10 passed before removal (unaffected pre-existing cases) | N/A — pure removal of now-mis-scoped cases, not a new-behavior RED | 10/10 passed (all remaining pre-existing cases in this file, unaffected by the correction) | N/A | The `useQueries` fan-out and `AgruparConsultaDialog` mount were deleted wholesale, not incrementally refactored |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | Backend: `./venv/Scripts/python.exe -m pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` → 25 passed. Frontend: `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → 9 files / 78 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` (`tsc -b && vite build`) → passed, no new route/chunk required since no route file changed in this correction |
| Rollback boundary | Backend: the 4 additive `services/pcp/negociacion/*.py` changes and the 3 new backend tests only — `GET /pcp/{pcp_id}/seleccion` and every other endpoint/model/repository function in this module are untouched. Frontend: `pcp.ts`'s new type+function, `pcp.test.ts`'s new test, `queryKeys.ts`'s new key, the mount-removal in `RenglonDetalle.tsx`/`RenglonDetalle.test.tsx`, the un-export in `ComparacionProveedoresTable.tsx`, and the mount-addition in `PcpDetalle.tsx`/`PcpDetalle.test.tsx` — `ConsultaDetalle.tsx`, `ConsultaDetalle.test.tsx`, `AgruparConsultaDialog.tsx`, and the nested consultation route are entirely unaffected |

- **Workload / PR boundary:** self-counted (backend files are tracked but hold accumulated uncommitted changes from earlier work units, so `git diff --numstat` cannot isolate this correction alone; frontend files remain untracked for the same reason) using exact `wc -l` before/after deltas captured during this session for every touched file: `repository.py` +21, `service.py` +32, `models.py` +10, `router.py` +13, `test_service.py` +80, `test_router.py` +37 (backend subtotal 193, all pure additions); `pcp.ts` +13, `pcp.test.ts` +14, `queryKeys.ts` +1, `PcpDetalle.tsx` +10, `PcpDetalle.test.tsx` +40 (pure additions), `RenglonDetalle.tsx` ≈24 changed (net −20, mostly removal), `RenglonDetalle.test.tsx` ≈51 changed (net −51, pure removal), `ComparacionProveedoresTable.tsx` ≈6 changed (net −4) (frontend subtotal ≈159). **Total ≈ 352 changed lines**, within the 400-line budget given to this correction. Rollback boundary is exactly the files listed above. No commit, push, PR, or later work unit (6.3 onward) was started or touched.
- **Design deviations / action-context warnings:** none beyond the gap this correction fixes. The new `GET /pcp/{pcp_id}/selecciones-agrupables` endpoint is additive and was not in `design.md`'s original file tree; it follows the exact same shape/role-gating as the frozen `GET /pcp/{pcp_id}/seleccion` endpoint it sits beside, and does not modify that endpoint's contract, callers, or tests.
- **Remaining implementation tasks:** unchanged — the next exact unchecked row is section 6.3's first item (as recorded above). Sugerencias, Legacy imports, and cross-unit validation remain unchecked in `tasks.md`.

## Work Unit 6 — Consultas 6.3 TRIANGULATE and REFACTOR (Work Unit 6 complete)

- **Status / safety:** consumed the orchestrator-injected, parent-authoritative OpenSpec `pcp-frontend` apply-ready context bounded to exactly section 6.3's two rows (29/39 tasks visibly `[x]` going in). Only `frontend/src/features/pcp/ConsultaDetalle.tsx`, `ConsultaDetalle.test.tsx`, `tasks.md`, and this record were touched; `AgruparConsultaDialog.tsx`, `lib/api/pcp.ts`, and `PcpDetalle.tsx` were read as context only, per the corrective rerun's already-established grouping scope, and not modified. Native ledger: `gentle-ai sdd-attempt acquire --token sha256:301d3bc9d9629240c5b9e8c209083bfc27220f23570c48db867f3fbab7821aa5 --request-id pcp-frontend-6-3-consultas-triangulate-refactor --work-unit consultas-6.3 --evidence-goal "TRIANGULATE+REFACTOR section 6.3 Consultas"` returned `state: proceed`; `gentle-ai sdd-attempt settle --token <same> --outcome passed ...` returned `state: complete`; a subsequent `gentle-ai sdd-attempt status` confirmed no `active_attempt` block remained (grepped the full JSON output for `active_attempt`, zero matches).
- **Completed / persisted checkboxes:** marked both section 6.3 rows visibly `[x]` in `tasks.md`. `grep -c "^- \[x\]" openspec/changes/pcp-frontend/tasks.md` → **29** (27 → 29, confirmed both by direct inspection and by the grep count).
- **TRIANGULATE — files changed and behavior:** added four `ConsultaDetalle.test.tsx` cases, all inside the existing frozen `AgruparConsultaDialog`/`ConsultaDetalle` describe blocks:
  1. `agruparConsultas` resolving with two `Consulta` objects for two distinct `proveedor_id`s (mirroring `SELECCIONES`'s existing multi-provider fixture, which the frozen 6.1 test already used to prove the *call* args but never asserted on a multi-item *result*) — asserts `AgruparConsultaDialog`'s success view renders one "Ver consulta de {proveedor_id}" link per returned consultation, not just one. This passed immediately (approval test): the dialog already `.map()`s over `mutation.data`, so no production change was needed or made.
  2. A `descargarPdfConsulta` rejection — asserts a `role="alert"` "No se pudo descargar el PDF." message appears and the "Descargar PDF" button stays enabled for retry. This also passed immediately: `ConsultaDetalle.tsx` already renders `descargaMutation.isError` into exactly that alert. Confirmed the error-handling path exists (per the objective's explicit instruction to check first), so this is a real approval test documenting already-correct behavior, not a redundant one — it is the first test in the suite to exercise the PDF-failure branch at all.
  3. A send-failure-then-retry-success sequence — `enviarConsulta` rejects once then resolves; clicking "Enviar consulta" twice asserts the alert clears and the "Consulta enviada." confirmation appears after the second click, with `enviarConsulta` called exactly twice. This is a stricter assertion than the frozen 6.1/6.2 "keeps the action usable" test (which only proved the button stays enabled), and it also passed immediately: TanStack Query's `useMutation` already resets `isError`/`isSuccess` on each new `mutate()` call, so no production change was needed.
  4. A send failure whose thrown `Error` message contains realistic messaging-adapter detail (read verbatim from `services/pcp/consultas/service.py::enviar_consulta`'s `ValidationError(f"No se pudo entregar la consulta '{consulta_id}' por ningún canal configurado: {errores}")`, which surfaces as a plain-string `ApiError.message` since `enviarConsulta` in `pcp.ts` performs no normalization) — asserts the alert never contains that adapter-specific text (`/whatsapp/i`, `/smtp/i`, `/canal configurado/i`).
- **REFACTOR — real defect found and fixed:** case 4 above was run RED-first, before any production change, and failed exactly as expected: `ConsultaDetalle.tsx`'s error branch rendered `envioMutation.error instanceof Error ? envioMutation.error.message : ...` directly, so a backend `ValidationError` about failed delivery channels (which legitimately can name the messaging adapter/channel that failed, per the backend code read above) would leak verbatim into the UI — a genuine violation of the spec's "MUST NOT surface any messaging-adapter-specific detail in the UI" requirement (`specs/pcp-ui-consultas/spec.md`, Requirement "Send Consultation Action Gated by Write Role"). Fixed by replacing the conditional `error.message` interpolation with the fixed literal `No se pudo enviar la consulta.`, removing all dependence on the thrown error's message content. This is the refactor's real payload — download cleanup (`iniciarDescarga`'s create-URL → append-anchor → click → remove-anchor → revoke-URL sequence, already fully self-cleaning since Work Unit 6.2) and the consultation route (`AgruparConsultaDialog` already links to the existing `/pcp/consultas/{id}` detail route, never a nonexistent index route) were both re-inspected and found already compliant, so no change was made to either — confirmed rather than invented, per this change's established pattern for a no-op sub-finding.
- **Verification:** safety net (before any edit) `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts` → **3 files / 40 tests passed**, matching the prior session's recorded 9-file/77-test baseline exactly for these three files. After the first three TRIANGULATE cases: same command → **43 passed**, all new. RED for the fourth (adapter-leak) case, added and run alone: `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx` → **12 tests, 1 failed** — the alert's received text was the raw backend sentence including `WhatsApp`/`SMTP`, confirming the leak was real, not hypothetical. GREEN after the one-line fix: same command → **12/12 passed**; broader command `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/features/pcp/PcpDetalle.test.tsx` → **2 files / 24 tests passed**. `cd frontend && npm run build` (`tsc -b && vite build`) → passed, twice (once after TRIANGULATE, once after the REFACTOR fix), no new route/chunk expected or produced since no route file changed. Full required suite `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **9 files / 82 tests passed** (78 baseline + 4 new). `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → passed (exit 0), only the same pre-existing CRLF conversion warnings on unrelated tracked files already logged in every prior entry.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 6.3 TRIANGULATE — multi-provider grouping, PDF-fetch failure, send retry | `frontend/src/features/pcp/ConsultaDetalle.test.tsx` | Component integration | 3 files / 40 tests passed | N/A — all three passed immediately as approval tests of already-correct behavior (confirmed, not assumed, per the objective's explicit instruction) | 43/43 passed after the three additions | Multi-consultation result rendering, PDF-download failure surfacing, and send-failure-then-success retry are three distinct paths beyond the frozen single-consultation/PDF-success/single-failure cases | Deferred to the 6.3 REFACTOR row below |
| 6.3 REFACTOR — adapter-detail leak in send-failure feedback | `frontend/src/features/pcp/ConsultaDetalle.test.tsx` | Component integration | 43/43 (post-TRIANGULATE) passed | 1 new case failed: the alert rendered the raw backend `ValidationError` sentence naming WhatsApp/SMTP channel failures verbatim | 12/12 (this file) and 82/82 (full PCP suite) passed after replacing the interpolated `error.message` with a fixed adapter-agnostic literal | N/A — one defect, one fix; download-cleanup and route-existence sub-findings were confirmed already compliant, not triangulated further | Confirmed no further refactor needed in `iniciarDescarga` or the consultation route after the fix |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/ConsultaDetalle.test.tsx src/features/pcp/PcpDetalle.test.tsx src/lib/api/pcp.test.ts` → 3 files / 43 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` (`tsc -b && vite build`) → passed; full suite `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → 9 files / 82 tests passed |
| Rollback boundary | `frontend/src/features/pcp/ConsultaDetalle.tsx` (the one-line error-message fix only) and `ConsultaDetalle.test.tsx` (the four new test cases only), plus the two section-6.3 checkboxes in `tasks.md` and this record — no other PCP file was touched |

- **Workload / PR boundary:** bounded Consultas-only 6.3 slice; authored delta is small — `ConsultaDetalle.test.tsx` +58/-0 (four new `it` blocks); `ConsultaDetalle.tsx` +1/-1 (the single-line message fix); `tasks.md` +2/-2 (two checkbox flips). **Total ≈ 64 changed lines**, well below the 400-line budget. Rollback boundary is exactly the two source/test files above. No commit, push, PR, or later work unit (Sugerencias, section 7) was started.
- **Design deviations / action-context warnings:** none — the adapter-detail-leak fix directly implements the spec's own explicit "MUST NOT surface any messaging-adapter-specific detail in the UI" requirement; it is a defect fix under the assigned REFACTOR row, not a deviation from it.
- **Work Unit 6 (Consultas) is now fully complete:** sections 6.1 RED, 6.2 GREEN, and 6.3 TRIANGULATE/REFACTOR are all visibly `[x]` in `tasks.md`.
- **Remaining implementation tasks:** the next exact unchecked row is section 7.1's first item: `- [ ] Create failing \`frontend/src/features/pcp/SugerenciasPanel.test.tsx\` cases for grouping and recent-price display, no-data state, read-role visibility, and the absence of any PCP mutation control; verify with \`cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx\`; rollback the new suggestions test file. <!-- sdd-owner: implementation -->`. Section 7 (Sugerencias), section 8 (Legacy imports), and section 9 (cross-unit validation) remain entirely unchecked in `tasks.md`.

## Work Unit 7 — Sugerencias 7.3 TRIANGULATE and REFACTOR (completes Work Unit 7)

- **Status / safety:** consumed the orchestrator-injected `pcp-frontend` OpenSpec apply-ready context for the bounded 7.3 row (31/39 complete going in, since 7.1 RED and 7.2 GREEN were already `[x]`). Ran the native ledger acquire (`work-unit: sugerencias-7.3-triangulate-refactor`, `evidence-goal`: suggestions tests for a valid recent-price row and multi-renglón quantity aggregation, plus a read-only isolation refactor, pass focused Vitest and build) with the supplied token, and settle exactly as instructed once verification completed; `sdd-attempt status` confirmed no `active_attempt` block remains after settle (the JSON jumps directly from `objective` to `attempts` with no `active_attempt` key), and the last attempt (ordinal 30, `sugerencias-7.3-triangulate-refactor`) reports `outcome: passed`. The workspace root was the sole allowed edit root; only `frontend/src/features/pcp/SugerenciasPanel.tsx`, `frontend/src/features/pcp/SugerenciasPanel.test.tsx`, `tasks.md`, and this record were touched.
- **Completed / persisted checkboxes:** marked both section 7.3 rows visibly `[x]` in `tasks.md`; `grep -c "^- \[x\]" openspec/changes/pcp-frontend/tasks.md` reports **33**. This completes Work Unit 7 (Sugerencias) entirely: 7.1 RED, 7.2 GREEN, and 7.3 TRIANGULATE/REFACTOR are all `[x]`.
- **TRIANGULATE — files changed and behavior:** read the frozen `SugerenciasPanel.test.tsx` (7.1/7.2) first, per instruction, before writing anything new. Its existing recent-price case already asserted a single row's proveedor, `mantenimiento_hasta`, `dias_restantes` (via the "días restantes" text), `cantidad_minima`/`cantidad_maxima`, and `precio_unitario` in full — so a redundant single-row approval test was skipped. Two genuinely new cases were added instead: (1) two simultaneous recent-price rows with distinct values, asserting `getAllByRole('listitem')` has length 2 and both suppliers'/dates'/prices' distinct text values are all present — this passed immediately (9/9 including the 8 frozen cases) because the existing `preciosRecientes.map(...)` already generalizes correctly to N items, so it is an approval-style confirmation of already-correct behavior, not a forced generalization; (2) a grouping suggestion spanning 4 `renglon_ids` and 3 `pcp_ids`, asserting the panel surfaces the renglón/PCP counts as visible text (not just `cantidad_agregada`) — this genuinely failed RED (`SugerenciasPanel.tsx` never rendered `renglon_ids`/`pcp_ids` at all, only `producto_id` and `cantidad_agregada`), confirming the spec's grouping suggestion was previously silently truncating the multi-renglón/multi-PCP scope from the UI. Fixed by adding one new `<p>` in the agrupación `<article>`: `{agrupacion.renglon_ids.length} renglones agrupados en {agrupacion.pcp_ids.length} PCPs`, rendered as a single flat text node (no nested `<strong>`) so the regex assertion matches exactly one element, following the same nested-vs-flat-text pattern already used elsewhere in the file for `cantidad_minima`/`cantidad_maxima`/`precio_unitario` (plain numbers wrapped in `<strong>` are asserted with an exact string match; numbers in a flat, unwrapped `<p>` are asserted with a substring regex) to avoid a "matches both the wrapping element and its nested child" ambiguity.
- **REFACTOR — investigation and explicit no-op finding:** re-read `SugerenciasPanel.tsx`, `RenglonDetalle.tsx`, and `queryKeys.ts` to verify isolation from renglón mutations. Confirmed all three required properties: (a) `pcpQueryKeys.agrupacion(renglonId)` → `['pcp', 'sugerencias', renglonId, 'agrupacion']` and `pcpQueryKeys.preciosRecientes(renglonId)` → `['pcp', 'sugerencias', renglonId, 'precios-recientes']` live under a distinct `'sugerencias'` key namespace, never overlapping with the mutation-affected `['pcp', pcpId, 'renglones', renglonId]` or `['pcp', pcpId, 'seleccion']` keys that `SeleccionProveedoresSection`/`ComparacionProveedoresTable`/`RegistrarResultadoDialog` invalidate on write — confirmed by reading `queryKeys.ts` directly, not inferred; (b) `RenglonDetalle.tsx` mounts `<SugerenciasPanel renglonId={renglonId} />` with only the plain `renglonId` route-param string — no `puedeEscribir`, `pcpId`, or any prop derived from a `useMutation` hook is passed; a repo-wide grep of `SugerenciasPanel.tsx` for `useMutation`/`puedeEscribir`/`pcpId` found zero production references (the sole match is the file's own JSDoc explaining why it deliberately omits `puedeEscribir`, written in 7.2); (c) `role="region" aria-label="Sugerencias del renglón"` is present and unchanged in all three render branches (loading, error, populated) both before and after the TRIANGULATE addition. This isolation was already fully correct from the 7.2 GREEN slice; **no refactor code change was made** because none was needed, confirmed explicitly per instruction rather than inventing an unnecessary change.
- **Verification:** safety net before edits `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx src/features/pcp/RenglonDetalle.test.tsx` → **2 files / 15 tests passed**, matching the recorded 7.2 GREEN state exactly. RED after adding the two TRIANGULATE cases: `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx` → **1 file, 1 failed / 9 passed** — the multi-recent-price case passed immediately (approval), the multi-renglón-aggregation case failed with `Unable to find an element with the text: /4 renglones agrupados/i`, confirmed by inspecting the printed DOM that the agrupación `<article>` had no such text. GREEN after the one `<p>` addition: same command → **1 file / 10 tests passed**. `cd frontend && npm run build` (`tsc -b && vite build`) → passed. REFACTOR verification (no code change): same focused command → **1 file / 10 tests passed**, unchanged. Full required PCP suite `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **10 files / 92 tests passed** (90 baseline + 2 new). `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → exit 0, only the same pre-existing CRLF conversion warnings on unrelated tracked files already logged in every prior entry.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 7.3 TRIANGULATE — multi-recent-price list and multi-renglón aggregation count | `frontend/src/features/pcp/SugerenciasPanel.test.tsx` | Component integration | 2 files / 15 tests passed | 1 new case failed: renglón/PCP count text absent from the agrupación article | 10/10 passed after adding the one flat-text `<p>` | Multi-item recent-price list (approval of existing `.map()` generalization) and multi-renglón/multi-PCP count display (genuinely new markup) are two distinct new assertions beyond the five frozen 7.1 cases | Deferred to the 7.3 REFACTOR row below |
| 7.3 REFACTOR — read-only isolation from renglón mutations | N/A (read-only investigation) | N/A | N/A | N/A | N/A | N/A | Confirmed already compliant on all three checked properties (distinct query-key namespace, no mutation-derived props, intact `role="region"` semantics); no code changed, explicitly reported rather than inventing a refactor |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/SugerenciasPanel.test.tsx` → 10/10 passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` (`tsc -b && vite build`) → passed; full suite `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → 10 files / 92 tests passed |
| Rollback boundary | `frontend/src/features/pcp/SugerenciasPanel.tsx` (only the one new `<p>` rendering renglón/PCP counts), `frontend/src/features/pcp/SugerenciasPanel.test.tsx` (only the two new `it` blocks), plus the two section-7.3 checkboxes in `tasks.md` and this record — no other PCP file was touched |

- **Workload / PR boundary:** bounded Sugerencias-only 7.3 slice; authored delta is small — `SugerenciasPanel.tsx` +3/-0 (one new `<p>`); `SugerenciasPanel.test.tsx` +44/-0 (two new `it` blocks); `tasks.md` +2/-2 (two checkbox flips). **Total ≈ 49 changed lines**, well below the 400-line budget. Rollback boundary is exactly the two source/test files above. No commit, push, PR, or later work unit (section 8, Legacy imports) was started.
- **Design deviations / action-context warnings:** none. The new renglón/PCP-count text is a UI-surfacing choice driven directly by the launch prompt's explicit requirement ("confirming `cantidad_agregada` and the renglón count are both surfaced meaningfully, not silently truncated"), not a deviation from `spec.md`'s "Quantity-grouping suggestion is shown" scenario, which only requires the aggregated quantity to be shown and does not prohibit additionally showing the renglón/PCP counts.
- **Work Unit 7 (Sugerencias) is now fully complete:** sections 7.1 RED, 7.2 GREEN, and 7.3 TRIANGULATE/REFACTOR are all visibly `[x]` in `tasks.md`.
- **Remaining implementation tasks:** the next exact unchecked row is section 8.1's first item: `- [ ] Create failing \`frontend/src/features/pcp/ImportLegacyPcp.test.tsx\` cases for write-role import submission, read-only route denial, human-readable result summary, and repeat-import updated-not-duplicated feedback; verify with \`cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx\`; rollback the new import test file. <!-- sdd-owner: implementation -->`. Section 8 (Legacy imports) and section 9 (cross-unit validation) remain entirely unchecked in `tasks.md`.

## Work Unit 8 — Legacy imports 8.1 RED

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (33/39 complete going in) for this bounded 8.1 RED-only subunit, native ledger token `sha256:d8b93976ad9c13f92b5bc71d7a9813689dc352f790aa69246ddfd127896e9f8d` reacquired via `gentle-ai sdd-attempt acquire`. The stated allowed edit root (`C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE`) was honored; no production code, API method, component, or route was created in this subunit.
- **Completed / persisted checkbox:** marked section 8.1's single row visibly `[x]` in `tasks.md`. `grep -c "^- \[x\]"` over `tasks.md` reports **34**.
- **Files changed:** created `frontend/src/features/pcp/ImportLegacyPcp.test.tsx` only; `tasks.md` and this record.
- **Test contract established (RED):** the new suite imports the not-yet-existing `ImportLegacyPcp` component and the not-yet-existing `@/routes/_authenticated.pcp.imports` route module, and mocks `importarPcpLegacy` from `@/lib/api/pcp` (also not yet exported). It defines four cases: (1) a write-role user selects a file (mocked via a `File` whose `.text()` resolves to a JSON-encoded `FilaImportPcpLegacy[]`, isolating the RED contract from real CSV/column-parsing logic per the launch instructions) and submits, asserting `importarPcpLegacy` is called with exactly the parsed rows; (2) a route-level test importing `ImportsRoute.options.beforeLoad` directly (mirroring `roles.test.ts`'s established route-guard pattern) and asserting it throws for `superadmin` (read-only for this screen) and does not throw for `compras` (write role); (3) a mixed `creado`/`actualizado` result renders a human-readable count summary (`"2 PCP creados"`, `"1 PCP actualizado"`) and never a raw JSON fragment (`"codigo_legacy"` text is asserted absent); (4) a second submission whose backend result flips `accion` from `creado` to `actualizado` is reflected with a distinct label/count, proving the UI surfaces the idempotent update distinction rather than a generic success message.
- **Verification:** safety net before edits — `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **10 files / 92 tests passed**, matching the recorded prior state. Required RED command — `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx` → **1 test file failed (0 tests)**, exactly `Failed to resolve import "./ImportLegacyPcp" from "src/features/pcp/ImportLegacyPcp.test.tsx". Does the file exist?` — the expected missing-module failure, not a syntax error in the test file itself. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → **exit 0**, reporting only the same pre-existing CRLF conversion warnings on unrelated tracked files already present before this subunit.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 8.1 RED legacy import screen and route guard | `frontend/src/features/pcp/ImportLegacyPcp.test.tsx` | Component integration + route guard | 10 files / 92 tests passed | Failed to resolve missing `./ImportLegacyPcp` and `@/routes/_authenticated.pcp.imports` modules | Not run — RED-only assigned subunit | Four distinct cases (submission body, route allow/deny, mixed-result summary, repeat-import distinction) prepared for the 8.2/8.3 GREEN and TRIANGULATE passes | None — no production code in this subunit |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx` → 1 file failed (0 tests), missing-module error only |
| Runtime harness command/scenario and exact result | N/A — RED-only subunit adds no production code and no runtime boundary; `npm run build` is deferred to the 8.2 GREEN row per the work-unit boundary in `tasks.md` |
| Rollback boundary | `frontend/src/features/pcp/ImportLegacyPcp.test.tsx` and the single 8.1 checkbox in `tasks.md` only |

- **Workload / PR boundary:** bounded RED-only Imports slice; the new test file is 108 lines, well under the 400-line budget. Rollback boundary is exactly the new test file plus the one task checkbox and this record. No commit, push, PR, or later section-8 row was started.
- **Design deviations / action-context warnings:** none. The JSON-based file-parsing boundary is an explicit RED-time simplification named by the launch instructions, not a spec/design deviation — the exact parsing format (CSV vs JSON) is left open by `spec.md`/`design.md`, and the real parsing logic remains unimplemented and untested until 8.2 (GREEN, which owns the component and API method) and 8.3 (TRIANGULATE/REFACTOR).
- **Remaining implementation tasks:** the next exact unchecked row is section 8.2's first item: `- [ ] Add the typed legacy-import method to \`frontend/src/lib/api/pcp.ts\`, implement \`frontend/src/features/pcp/ImportLegacyPcp.tsx\`, and create \`frontend/src/routes/_authenticated.pcp.imports.tsx\` with an explicit write-role guard; render result counts/statuses without raw API payloads; verify the import RED cases pass; rollback the import screen, route, and API addition. <!-- sdd-owner: implementation -->`. Section 8.3 and section 9 (cross-unit validation) remain entirely unchecked in `tasks.md`.

## Work Unit 8 — Legacy imports 8.2 GREEN

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (34/39 complete going in) for this bounded 8.2 GREEN-only row, native ledger token `sha256:54ce1396780e7957d5835786cab036c3daeec9a3ca8da4fcc3c5a18e8833c4e6` (cumulative-line counter reset by explicit user authorization, starting fresh at 0 for this objective). The sole allowed edit root (`C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE`) was honored. The frozen `ImportLegacyPcp.test.tsx` (from 8.1 RED) was read but not modified. Native ledger: `gentle-ai sdd-attempt acquire` (same token) returned `state: proceed`; `gentle-ai sdd-attempt settle --outcome passed ...` first returned `blocked(undeclared_untracked)` listing ~60 pre-existing untracked files from earlier work units plus this attempt's own new files, with a fresh `expected-untracked-inventory` hash; the retry used `--untracked-scope=exclude` with that fresh hash (this attempt's files are additive slices of already-untracked directories/files from prior work units, not newly-untracked paths of their own) and returned `state: complete`. `gentle-ai sdd-attempt status` was then grepped for `active_attempt` — zero matches, confirming the ledger closed cleanly.
- **Completed / persisted checkbox:** marked section 8.2's single row visibly `[x]` in `tasks.md`. `grep -c "^- \[x\]" openspec/changes/pcp-frontend/tasks.md` → **35**.
- **Files changed:** modified `frontend/src/lib/api/pcp.ts` (added `ProcesoComercialLegacy`, `FilaImportPcpLegacy`, `ImportPcpLegacyResultado` types and `importarPcpLegacy`); created `frontend/src/features/pcp/ImportLegacyPcp.tsx` and `frontend/src/routes/_authenticated.pcp.imports.tsx`; regenerated `frontend/src/routeTree.gen.ts`; `tasks.md` and this record.
- **Behavior:** `importarPcpLegacy(filas)` POSTs the exact `{filas}` body to `/pcp/imports/legacy` via `presupuestacionFetch`, mirroring the existing typed-client convention (explicit body construction, no spreads). `ImportLegacyPcp.tsx` renders a file input labeled "Archivo legado" (accessible name matches `/archivo/i`), parses the selected file client-side via `File.text()` + `JSON.parse` into `FilaImportPcpLegacy[]` (the exact format is left open by spec/design per the 8.1 RED author's note; JSON keeps the RED/GREEN focus on the component↔API contract, not column-parsing logic — explicitly deferred, not invented, to 8.3), and submits via a `useMutation` wrapping `importarPcpLegacy`. On success it renders a pluralized human-readable summary (`"N PCP creado(s)"` / `"N PCP actualizado(s)"`), never a raw JSON dump, and the summary is fully replaced (not accumulated) by each new submission so a repeat import correctly shows only the latest `accion` counts. `_authenticated.pcp.imports.tsx` is the one PCP route gated by `PCP_WRITE_ROLES` instead of `PCP_READ_ROLES` (mirrors `catalogo`'s `requireRole` route-guard shape exactly), so `superadmin` — read-only for PCP — is denied navigation while `compras`/`admin`/`gerencia` are admitted.
- **Deviation found and fixed during GREEN:** the file `<input>` initially carried an HTML5 `required` attribute in addition to the `disabled={!archivo}` button gate. This caused jsdom's native form-validation to block the `submit` event after `fireEvent.change` set `target.files` (jsdom does not treat that assignment as satisfying the file input's validity constraint), so `importarPcpLegacy` was never called and 3 of 4 frozen tests failed RED-for-the-wrong-reason. Removed the redundant `required` attribute — the button's `disabled` state already gates submission — which is not a spec/design deviation, only an implementation-detail fix discovered by running the tests.
- **Verification:** safety net before edits — `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **10 files / 92 tests passed**, matching the recorded 8.1 RED state exactly (`ImportLegacyPcp.test.tsx` still failing to resolve the missing module). Required focused command after implementation — `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx` → **1 file / 4 tests passed** (all four frozen 8.1 RED cases: write-role submission with parsed filas, route guard allow/deny, human-readable mixed-result summary with no raw `"codigo_legacy"` text, and repeat-import updated-not-duplicated distinction). Full required PCP suite — same safety-net command → **11 files / 96 tests passed** (92 + 4 new). `cd frontend && npx vite build` (regenerates `routeTree.gen.ts`; the plugin only rewrites the route tree during a Vite build/dev pass, so this plain regeneration step had to run once before `tsc -b` could type-check the new route) → passed, produced the new `_authenticated.pcp.imports-*.js` chunk. `cd frontend && npm run build` (`tsc -b && vite build`) → passed; confirmed `/_authenticated/pcp/imports` present in `routeTree.gen.ts` (`AuthenticatedPcpImportsRoute`, `fullPath: '/pcp/imports'`) via direct grep. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → exit 0, only the same pre-existing CRLF conversion warnings on unrelated tracked files already logged in every prior entry.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 8.2 typed import API, screen, and write-role-only route | `frontend/src/features/pcp/ImportLegacyPcp.test.tsx` | Component integration + route guard + API contract | 10 files / 92 tests passed (8.1 RED state unchanged) | 3/4 frozen cases failed for the `required`-attribute jsdom validation reason above before the fix | 4/4 passed after removing the redundant `required` attribute | Deferred to 8.3 (this row is GREEN-only per the work-unit boundary) | Deferred to 8.3 |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx` → 1 file / 4 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` (`tsc -b && vite build`) → passed; new route `/_authenticated/pcp/imports` confirmed present in `routeTree.gen.ts` and produced its own build chunk `_authenticated.pcp.imports-*.js` |
| Rollback boundary | The `importarPcpLegacy` type/function block in `pcp.ts`, `ImportLegacyPcp.tsx`, `_authenticated.pcp.imports.tsx`, the regenerated `routeTree.gen.ts` entry, and the single 8.2 checkbox in `tasks.md` — no other PCP file was touched |

- **Workload / PR boundary:** bounded GREEN-only Imports slice; authored source delta is small — `pcp.ts` +38 (isolated additive block, lines 405–442); `ImportLegacyPcp.tsx` +73 (new file); `_authenticated.pcp.imports.tsx` +12 (new file); `tasks.md` +1/-1 (one checkbox flip). **Total ≈ 124 authored changed lines**, well below the 400-line budget; the generated `routeTree.gen.ts` diff is excluded from authored risk count per the review-budget guard, consistent with every prior route-adding slice in this change. Rollback boundary is exactly the files listed above. No commit, push, PR, or later task (8.3 or section 9) was started.
- **Design deviations / action-context warnings:** none beyond the `required`-attribute jsdom fix documented above, which is an implementation detail, not a spec/design deviation — the component still requires a selected file before the mutation can fire, enforced by the button's `disabled` state.
- **Remaining implementation tasks:** the next exact unchecked rows are section 8.3's two items: `- [ ] Add import tests for mixed created/updated result rows and API failure recovery, then run focused Vitest plus \`cd frontend && npm run build\`; rollback the added import edge cases. <!-- sdd-owner: implementation -->` and `- [ ] Refactor import result presentation without changing idempotency semantics or broadening route access; verify the focused import test command; rollback the import-only refactor. <!-- sdd-owner: implementation -->`. Section 9 (cross-unit validation) remains entirely unchecked in `tasks.md`.

## Work Unit 8 — Legacy imports 8.3 TRIANGULATE and REFACTOR (Work Unit 8 complete)

- **Status / safety:** consumed the parent-authoritative OpenSpec `pcp-frontend` apply-ready context (35/39 complete going in) for this bounded final 8.3 row, native ledger token `sha256:529299b14a1d49d569a264c35a40537990fb7b78da92c4cca918f4f03e3c7652`. `gentle-ai sdd-attempt acquire` (same token, with the required `--request-id`, `--work-unit`, `--evidence-goal` flags this CLI version now requires) returned `state: proceed` against the pre-declared objective `imports-8.3-triangulate-refactor`. The sole allowed edit root (`C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE`) was honored. Only `ImportLegacyPcp.tsx`, `ImportLegacyPcp.test.tsx`, `tasks.md`, and this record were touched; the frozen route guard file `_authenticated.pcp.imports.tsx` (`PCP_WRITE_ROLES`) was read but not modified, confirmed by an empty `git diff` on that path after the edits.
- **Completed / persisted checkboxes:** marked both section 8.3 rows visibly `[x]` in `tasks.md`. `grep -c "^- \[x\]" openspec/changes/pcp-frontend/tasks.md` → **37**. This completes Work Unit 8 (Legacy imports) entirely — only Work Unit 9 (cross-unit validation, 2 rows) remains in the whole `pcp-frontend` change.
- **TRIANGULATE finding (honest, non-fabricated RED):** read the frozen 8.1/8.2 tests first per the launch instructions; the existing "muestra un resumen legible" case already exercises a mixed `creado`+`actualizado` response (2 creado + 1 actualizado) in one array, so a redundant approval of that exact shape was avoided. Added two genuinely new cases instead: (1) an uneven 3-creado/1-actualizado mix in one response, asserted with anchored exact-text regex (`/^3 pcp creados$/i`, `/^1 pcp actualizado$/i`) plus explicit negative assertions (`4 pcp`, `2 pcp actualizado` absent) to prove the counts are numerically exact, not just "some of each"; (2) API failure recovery — `importarPcpLegacy` rejecting with an `Error` containing a fake stack-trace string, asserting a `role="alert"` generic message is shown, the leaked detail text is NOT present, the submit button is NOT stuck disabled, and a second click after a successful mock actually calls `importarPcpLegacy` a second time and renders the new success summary while the alert clears. Both new tests **passed on first run** — the launch instructions asked to check whether `ImportLegacyPcp.tsx` had any `onError`/error-rendering branch at all; it already did (`mutation.isError` → generic `role="alert"` message, `disabled={!archivo || mutation.isPending}` only — no permanent lock), added during 8.2 GREEN following the same pattern already established in `ConsultaDetalle.tsx`'s `envioMutation.isError` and `ComparacionProveedoresTable.tsx`'s `proveedorConError` handling (verified by rereading both files). So there was no real implementation gap to close; these are legitimate approval/confirmation tests per the strict-tdd module's Approval Testing pattern, not a shortcut — each assertion calls production code and would fail if the implementation regressed (verified non-triviality: exact-anchor regex, retry call-count assertion, leaked-detail exclusion).
- **REFACTOR:** extracted two pure helpers in `ImportLegacyPcp.tsx` — `contarPorAccion(resultados)` (replaces the two inline `.filter(...).length` expressions) and `formatearConteo(cantidad, singular, plural)` (replaces the duplicated singular/plural ternary that appeared once per accion type). Rendered text is byte-identical (`"N PCP creado(s)"` / `"N PCP actualizado(s)"`); no route, API body, or idempotency semantics were touched.
- **Verification:** safety net before edits — `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx` → **1 file / 4 tests passed** (unchanged from 8.2). After adding the two TRIANGULATE cases — same command → **1 file / 6 tests passed** (no RED — see finding above, both new cases passed against the pre-existing implementation). After REFACTOR — same command → **1 file / 6 tests passed**. Full required PCP suite — `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **11 files / 98 tests passed** (92 prior + 6 in this file). `cd frontend && npm run build` → passed (`tsc -b && vite build`), new/changed chunks built cleanly. `git -C "C:\Users\LAUREANO\OneDrive\Escritorio\ExtraccionDatosNE" diff --check` → exit 0, only the same pre-existing CRLF conversion warnings on unrelated tracked files logged in every prior entry.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 8.3 TRIANGULATE + REFACTOR — import edge cases and presentation cleanup | `frontend/src/features/pcp/ImportLegacyPcp.test.tsx` | Component integration | 4/4 passed | No gap found: both new cases (uneven mix, API-failure recovery) passed against the existing 8.2 GREEN implementation, which already had `mutation.isError` handling and no-permanent-disable behavior | 6/6 passed (confirmation, not a fix) | Uneven 3/1 mix with exact-anchor negative assertions; API rejection with leaked-detail exclusion and retry-call-count proof | Extracted `contarPorAccion` and `formatearConteo`; 6/6 still passing after each step |

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Focused test command and exact result | `cd frontend && npm run test -- src/features/pcp/ImportLegacyPcp.test.tsx` → 1 file / 6 tests passed |
| Runtime harness command/scenario and exact result | `cd frontend && npm run build` → `tsc -b && vite build` passed |
| Rollback boundary | `ImportLegacyPcp.tsx` (helper extraction only), `ImportLegacyPcp.test.tsx` (two new `it` blocks), and the two 8.3 checkboxes in `tasks.md` — no route, API, or other component file touched |

- **Workload / PR boundary:** bounded final 8.3 slice; authored delta is small — `ImportLegacyPcp.test.tsx` +40 lines (two new test blocks); `ImportLegacyPcp.tsx` +14/-4 (helper extraction, net +10); `tasks.md` +2/-2 (two checkbox flips). **Total ≈ 54 authored changed lines**, well below both the 400-line change budget and this objective's own 400-line ledger cap (settled with 0 recorded changed lines per the runtime ledger, since the ledger tracks a different counting window than this authored-line report). Rollback boundary is exactly the files listed above. No commit, push, or PR was created.
- **Design deviations / action-context warnings:** none. The TRIANGULATE "no RED found" outcome is reported transparently rather than fabricating a gap; it reflects that 8.2 GREEN already implemented the recovery pattern by following the established `ConsultaDetalle.tsx`/`ComparacionProveedoresTable.tsx` convention.
- **Native runtime ledger:** `gentle-ai sdd-attempt acquire` (token `sha256:529299b14a1d49d569a264c35a40537990fb7b78da92c4cca918f4f03e3c7652`, `--request-id pcp-frontend-8-3-triangulate-refactor`, `--work-unit "Work Unit 8 - Legacy imports 8.3 TRIANGULATE and REFACTOR"`, `--evidence-goal "Add mixed-result and API-failure-recovery import tests, then refactor import result presentation only"`) → `state: proceed` against the pre-declared objective `imports-8.3-triangulate-refactor`. `gentle-ai sdd-attempt settle` (same token, `--request-id pcp-frontend-8-3-triangulate-refactor-settle`, `--outcome passed`, `--evidence-revision sha256:d6bd0b58...ccac864` (sha256 of the two changed source/test file contents), `--diagnosis`, `--harness-disposition reused`, `--cleanup-evidence`, `--process-evidence` describing the exact test/build results above) → `state: complete`. `gentle-ai sdd-attempt status --change pcp-frontend` afterward was grepped for `active_attempt` → **0 matches**, confirming the ledger closed cleanly with no dangling attempt.
- **Remaining implementation tasks:** only Work Unit 9 (cross-unit validation) remains, both rows still `[ ]`: `- [ ] Run the frontend PCP suite and production build with \`cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts && npm run build\`, resolving only PCP-change failures; rollback by reverting the failing unit at its documented work-unit boundary. <!-- sdd-owner: implementation -->` and `- [ ] Run the backend selection prerequisite and repository-required suite with \`pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py\` followed by \`pytest --cov=services --cov-report=html\`; record any unrelated baseline failures separately rather than modifying unrelated untracked artifacts; rollback only PCP selection-persistence changes if regression is attributable to this change. <!-- sdd-owner: implementation -->`

## Work Unit 9 — Cross-unit validation (change complete: 39/39)

- **Status / safety:** run directly by the orchestrator (mechanical verification, not implementation — no research or design decision involved, no sdd-apply delegation needed per the delegation rules' bounded-action allowance for tests/builds). No SDD status, authority, or review command was run.
- **Row 1 — frontend PCP suite + build:** `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts` → **11 files / 98 tests passed**. `cd frontend && npm run build` → `tsc -b && vite build` passed cleanly, no errors. No PCP-change failure to resolve.
- **Row 2 — backend selection prerequisite + full-repo coverage:**
  - `pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` → **25 passed**.
  - `pytest --cov=services --cov-report=html -q` failed with `unrecognized arguments: --cov=services --cov-report=html` — the `pytest-cov` plugin was not installed in this venv and is absent from `requirements*.txt`. Asked the user; explicitly authorized installing it (`pip install pytest-cov`, dev-only test tooling, no production/runtime dependency added). Installed `pytest-cov==7.1.0` + `coverage==7.16.0`, then reran the exact task command.
  - Full-repo run: **660 passed, 7 failed**, `htmlcov/index.html` generated (91.8 KB). All 7 failures are in `tests/core/test_stock.py` (3 concurrency cases) and `tests/usuarios/test_service.py` (4 cases) — confirmed via `git status`/`git diff --stat` that neither file has any local modification, and the last commit touching either (`ce4414b feat: reemplazar alta con password por invitacion por email en usuarios`) is unrelated to `pcp-frontend`. Re-ran the 7 in isolation: 6/7 failed again identically (one concurrency case passed the second time, consistent with genuine timing-sensitive flakiness in concurrent-stock tests hitting the real Supabase test project). Inspected `test_superadmin_crea_admin` directly: it fails on a real `ConflictError` from a 429 rate limit on Supabase Auth's invite-email endpoint (`services/presupuestacion/usuarios/repository.py:28`) — an infrastructure/test-environment limitation (the test project's email-send quota), not a code defect, and unrelated to PCP selection-persistence or any file this change touches.
  - Per the task's own instruction ("record any unrelated baseline failures separately rather than modifying unrelated untracked artifacts"), these 7 are recorded here as a pre-existing baseline condition and were not investigated further or fixed — out of scope for `pcp-frontend`.
- **Verification:** both rows independently confirmed passing/complete as described above; `git diff --check` clean (pre-existing CRLF warnings only).

### Work Unit Evidence
| Evidence | Value |
|---|---|
| Frontend focused test + build | `cd frontend && npm run test -- src/features/pcp src/lib/api/pcp.test.ts && npm run build` → 11 files/98 tests passed, build passed |
| Backend PCP-scoped suite | `pytest tests/pcp/negociacion/test_service.py tests/pcp/negociacion/test_router.py -q` → 25 passed |
| Backend full-repo coverage | `pytest --cov=services --cov-report=html -q` → 660 passed, 7 failed (all pre-existing/unrelated, documented above), `htmlcov/` generated |
| Rollback boundary | N/A — this row is verification-only; no source file was changed. `pytest-cov`/`coverage` were added to the local venv only (not committed to `requirements*.txt`), reversible with `pip uninstall pytest-cov coverage` if ever needed |

- **Workload / PR boundary:** verification-only, zero authored source lines. Both `tasks.md` checkboxes for Work Unit 9 are now `[x]`.
- **Design deviations / action-context warnings:** one environment gap found and resolved with explicit user authorization — `pytest-cov` was missing from the project's declared dependencies; installed into the existing venv only, not added to `requirements*.txt` (a maintainer should decide whether to formalize it there going forward).
- **Change status: `pcp-frontend` is now 39/39 tasks complete.** Every work unit (1 through 9) is `[x]`. No `sdd-verify`, delivery, commit, push, or PR action was taken as part of this — those remain separate, explicitly human-owned next steps.
